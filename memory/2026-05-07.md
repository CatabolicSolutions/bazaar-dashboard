- 06:30 MDT cron ran `scripts/post_daily_macro_report.sh` successfully.
- Updated `out/daily_macro_report.md` and posted the finished daily macro report to Discord channel `1484407313635868715` (message `1501924275907727442`).
- Rotator durable state: live production service was cut over from `eth-scalper.service` to `uniswap-rotator.service` on the Bazaar droplet. Old `eth-scalper.service` is now stopped/disabled; canonical active runtime is `/var/www/bazaar/UniSwap_Rotator` running `python3 -m UniSwap_Rotator.runtime.live_runner`.
- Rotator architecture continuity: actual live strategy brain remains the committed `dual_rotator` logic (v2 rotate-signal path) originally from `eth_scalper`; `UniSwap_Rotator` was bridged to call that same live decision engine instead of inventing a new strategy. This was the key fix to avoid architecture swapping.
- Critical commits/changes: live droplet repo received commit `2e3129ae` with message `rotator: track live dual main and v2 rotate-signal logic` after git permission repair on `/var/www/bazaar/.git`.
- Major runtime fixes applied: `dual_main.py` was updated to maintain/pass `rotate_state`, `q_history`, and deque-backed histories to match v2 `dual_rotator.py`; rotate-signal history indexing bug in `detect_rotate_signal` was patched after live `IndexError` surfaced.
- UniSwap_Rotator bridge work: added canonical runtime bridge files so `UniSwap_Rotator` can infer `target_side` by calling the live rotator brain, plus a bootable `runtime/live_runner.py`; patched `runtime/state/store.py` to coerce persisted `side` strings back into `Side` enums.
- Config canonization: `UniSwap_Rotator` env loading now prefers `/var/www/bazaar/UniSwap_Rotator/.env` first (with fallback to legacy env paths), and a real local `.env` was created so the canonical runtime no longer depends on `eth_scalper/.env` first.
- Important operational lesson from Conor: for rotator, only one architecture matters — the live bot. Replay is diagnostic only. Safe sequence is diagnose live bot -> patch live bot -> replay-validate same logic -> commit -> deploy -> verify; renaming/naming cleanup must happen only after the correct live logic is canonized.
