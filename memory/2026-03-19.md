# 2026-03-19

- Continued the probability-first Kalshi refactor.
- Implemented the next discovery/selection shove:
  - shorter resolution preference (72h max)
  - stricter simple-binary preference via lower complexity threshold
  - ranking now favors nearer-term/simple contracts before liquidity metadata
- Live raw-API pass still returned `No Trade`.
- Current rejection profile after shove:
  - `too_far_to_resolution`: 74
  - `reject_hard`: 26
- This confirms the current sampled Kalshi open feed is still dominated by longer-dated and structurally messy contracts rather than near-term clean binaries.
- Important progress: the engine is now failing for the right reasons (contract horizon / complexity), not because of missing auth, stale category rejection, or shallow SDK quote handling.
