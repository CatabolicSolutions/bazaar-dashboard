
## Recovery-grade continuation note — 2026-04-28 02:24-02:28 MDT

### Fee model confirmed by operator
- Conor confirmed working **round-trip swap fee baseline = $0.07**.
- Treat this as canonical until enough measured live receipts justify lowering it further.
- Operational interpretation:
  - approximately `$0.035` per side
  - fee gating should support higher-frequency micro-rotation rather than conservative swing thresholds

### Fee / trigger / frequency posture now adopted
Bot was tuned for higher trade count and capital accumulation:
- `DEFAULT_EST_ROUNDTRIP_FEE_USD = 0.07`
- `MIN_NET_EDGE_BUFFER_USD = 0.00`
- `VOL_FLOOR = 0.10`
- `VOL_CAP = 0.25`
- `VOL_MULTIPLIER = 1.2`
- `SIGNAL_CONFIRM = 2` retained

This is the current philosophy:
- prioritize valid repeated rotations
- let trade frequency compound capital
- do not over-weight single-trade edge if fees are low and structure is clean

### Round-trip expectancy gate (current logic)
Current fee gate now evaluates cycle expectancy, not just point-in-time EMA separation:
- if holding `WETH`: estimate value of **sell now -> rebuy at modeled lower target**
- if holding `USDC`: estimate value of **buy now -> resell at modeled higher target**
- gate compares that expected full-cycle edge against the fee baseline

### Manual sell override completed successfully
Conor explicitly directed reevaluate + execute sell.
Manual override sell was executed after reevaluation.

Canonical trade outcome:
- state flipped to `USDC`
- `entry_price = 2288.445`
- `trade_count = 7`
- gas cost recorded approximately `$0.002564`
- tx hash: `0x95d5839156aa19510e5dc3e3c9cbe94b92854e16c46a18d5852603f19dda3082`
- receipt block: `45287457`

### Current canonical post-sell bot state
As of latest tuning pass:
- `side = USDC`
- `entry_price = 2288.445`
- `USDC ≈ 248.11`
- `WETH = 0`
- latest EMA references around:
  - `EMA_12 ≈ 2287.15`
  - `EMA_50 ≈ 2287.05`
- modeled rebuy target after sell was roughly `2282.58` at that moment

### Logging / iteration / backtest posture
The machine should now be left running and logging real outcomes for later refinement.
Primary files to use for iteration/backtest/review:
- `/var/www/bazaar/eth_scalper/logs/trades.jsonl`
- `/var/www/bazaar/eth_scalper/logs/failed_swaps.jsonl`
- `/var/www/bazaar/logs/eth_scalper_status.json`
- `/var/www/bazaar/eth_scalper/state/scalp_state.json`

What the next iteration model should ingest:
- trade timestamp
- side before/after
- entry / exit
- trigger level
- EMA references
- gas used
- effective gas price
- gas cost USD
- gross P&L
- net P&L after gas
- failed swap reasons / revert patterns

### Durable strategy thesis to preserve
Conor’s explicit directive:
- **volume of trades and cumulative capital increase can outperform waiting for larger isolated edge percentages**
- the system should favor many clean micro-rotation opportunities when fees are genuinely low
- long-run accumulation is treated as an exponential multiplier built from repeated retained gains

This is now a core design assumption for future tuning, not a side comment.

- User explicitly approved full live cutover of ETH scalper execution from 1inch-routed execution toward direct Uniswap V3 on Base and approved using the deepest UniV3 pool.
- Investigation confirmed live executor had been using direct 1inch API execution (not MetaMask retail swap UI), with current slippage tolerances still too loose for micro-rotation economics.
- During the cutover discussion, user preference remained strongly anti-churn / anti-hidden-friction and pro-direct routing with tighter execution control.
- User encouraged continued push toward UniV3 execution path: “I like the direction you're going in, keep pushing to UniV3”.
