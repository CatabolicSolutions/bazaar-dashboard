# 2026-03-16

- Patched `scripts/tradier_strategy_processor_v2.py` for Phase 1 / Option A execution.
- Fixed DTE truthfulness: requested DTE and actual DTE are now separated, with fallback labels like `4DTE (requested 0DTE, fallback)` instead of incorrectly labeling nearest available expiry as 0DTE/1DTE.
- Tightened Tradier strategy filters substantially:
  - scalping buy now uses strict delta bands and realistic distance-to-underlying limits
  - credit spread sell now enforces OTM directionality, liquidity checks, narrower delta range, and spread-width checks
- Added ranking and capping:
  - scalping capped to 4 candidates per symbol/requested-DTE
  - credit spreads capped to 3 candidates per symbol/requested-DTE
  - fallback shortlist capped to 2 when no strict candidates survive
- Added liquidity/spread sanity checks to reduce junk candidates.
- Smoke test succeeded: output volume dropped from ~3000 tickets to 142 total tickets in a direct test run, while surfacing explicit fallback-expiry labels.
