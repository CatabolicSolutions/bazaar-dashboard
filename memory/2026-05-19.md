# 2026-05-19 — AGORA Hub Redesign & CFM Operational Refinements

## AGORA Hub (Root Web Page)
- Conor explicitly wants **AGORA** as the single unified hub — all subsystems in one polished page at `https://137.184.144.196/`
- First version was slapped together; Conor said "This looks like shit" and called out bad design + stale data
- **Key lesson:** Do not half-ass web design. He wants something that "works and you can be proud of." Take the time to build a proper UI.
- Redesigned with: Inter font stack, JetBrains Mono for numbers/tables, proper layered dark theme, service health dots (green/red per subsystem), signal banners with directional arrows and confidence badges, responsive grid layout, loading/empty states
- Data fixed: removed stale NVDA and IWM positions from position_monitor_report.json; position panel now shows clean empty state instead of wrong data

## Remaining Position Pipeline Issue
- QQQ position doesn't appear in AGORA because the position monitor only has stale cached data
- Need to wire the position monitor to query Tradier's live API for active positions instead of relying on the file
- This is a separate pipeline fix, not a UI issue

## Droplet OOM Problems
- 1GB RAM droplet continues to choke — SSH connections time out, nginx returns 502 when backend goes down
- Recovery is automatic after ~20-30s but unreliable for SSH commands
- Web server (cockpit on port 8765) recovers independently; AGORA page loads once backend comes back

## CFM Runner Updates
- Per-asset sweep parameters deployed: BTC 0.10%, ETH 0.10%, SOL 0.15%, GOLD 0.05%, MAG7C 0.08%
- Runner cycles all 5 mids (BTC $76,890, ETH $2,132, SOL $85.16, GOLD $4,541, MAG7C $2,886)
- No actionable setups detected in current cycles
- Conor wants 3-7% stop range (not 0.2%) — sweep thresholds are for entry detection, not stops
- Re-entry delay for DEX bot: 3 hours after exit
- Live trading enabled, guardrails: 75% max risk, 3x leverage, 1 max position

## Backtest Harness
- Conor greenlit building a backtest matrix to tune per-asset parameters
- Strategy: use historical candle data (60 candles max from fetch) + parameterize sweep/stop/reclaim thresholds per asset
- Currently have 5 assets with distinct sweep params; need harvest rig to accumulate more data
- Backtest harness build is queued for upcoming session(s)
