- ETH/BTC rotator workstream progressed from zero-trade harness to active dedicated rotate-signal architecture, then to first positive replay via cleanup-pass baseline.
- Positive replay baseline found: about +4.31% ETH-equivalent with conservative ETH/BTC selection, suppressed churn, no explicit in-position rotates firing; candidate controls included rotate persistence=2, post-hold=8, arm-wait suppression during rotate contexts, and conservative rotate thresholds.
- Important lesson: counterfactual rotate edge at 12-24 bars can be real while full-strategy PnL remains negative if entry/exit churn and execution-layer interference harvest the edge poorly.
- Live deployment truth discovered: actual droplet runtime is alfred-deploy@137.184.144.196 under /var/www/bazaar/eth_scalper, service eth-scalper.service, runner python3 -m bot.dual_main, strategy core bot/dual_rotator.py; not the workspace bot.main path.
- Droplet service was already unhealthy before final deployment attempt, crash-looping; restart from mobile confirmed current failure is ModuleNotFoundError: No module named 'config.settings' in bot/dual_main.py line 10 after service restart.
- Next concrete fix: patch droplet bot/dual_main.py with the same path bootstrap/import handling pattern used by workspace bot/main.py so config.settings resolves under python3 -m bot.dual_main, then restart eth-scalper.service and verify logs.
- ETH scalper droplet repair on `alfred-deploy@137.184.144.196`: confirmed `sudo -n` workable remotely; root startup failure was `ModuleNotFoundError: No module named 'config.settings'` under `eth-scalper.service` (`ExecStart=/usr/bin/python3 -m bot.dual_main`).
- Live runtime repair chain on droplet restored missing source files and boot pathing: `config/settings.py`, `execution/live_executor.py`, `config/logger.py`, plus bootstrap/path fixes in `bot/dual_main.py`; service `eth-scalper.service` was brought back to `active (running)` with logging.
- Important durability note: live droplet runtime is the dual architecture under `/var/www/bazaar/eth_scalper/bot/dual_main.py` and `/var/www/bazaar/eth_scalper/bot/dual_rotator.py`, not the workspace single-runner path; claims about live deployment should only be made after restarting and verifying that exact service on the droplet.
- User asked whether new tuned logic was live; confirmed yes after service boot and log validation.
- Later live-performance review showed unhealthy behavior despite service uptime: repeated `EXIT_WETH` / `stale loss` churn in logs, a reporting/runtime bug `unsupported format string passed to dict.__format__`, suspicious side/accounting inconsistency (`BUY_BTC_81317` ended with `side_after: "WETH"` and `weth_after: 0.0`), and printed cumulative P&L figures were deemed untrustworthy.
- Durable operational truth: for the ETH scalper, `service running` does not imply `strategy healthy`; current live accounting/decision telemetry is unreliable and should not be trusted for P&L until state/accounting and churn bugs are repaired.
