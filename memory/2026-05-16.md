- Built and deployed a slim `Agora-Algo` hub consolidating Tradier monitored-position state and Uniswap rotator state into 3 sections; explicit `/Agora` route was added and live service restarted on droplet.
- Restored Agora upstream data by wiring/populating Bazaar-fed endpoints for `position_monitor_report.json` and `rotator_hub_state.json`; page shell was initially live before data was fixed.
- Added Tradier leaders/score-card section to Agora using existing leaders JSON/board artifacts.
- Rotator audit conclusion: cross-asset ETH/BTC rotation was structurally leaking PnL; replay audit showed roughly -11.29% ETH-equivalent with high churn, leading to decision to pivot away from cross-asset rotation.
- Live crypto runtime confirmed as `/var/www/uniswap_rotator` `python3 -m runtime.live_runner` / `uniswap-rotator.service`; real cutovers must target this runtime, not legacy local eth_scalper scripts.
- Implemented BTC-only live side-selection path (BTC/USDC only, no WETH targeting) and restarted service; initial cutover inherited broken/legacy state such as `entry_BTC = 0.0`, causing bad outputs like `exit$0`.
- Patched BTC-only state hydration so held BTC positions always get sane entry/peak basis before exit math runs; after fix, live logs emitted coherent fields like real exit targets, extension, peak, and retrace.
- Harness/matrix sweeps on the first BTC-only coefficient family found no true winner; best tested family was still negative (~-4.27%) with too much churn, so coefficient tuning alone on that skeleton is insufficient for strong confidence.
- Old ETH reversal/scalp artifacts exist and were identified as a better substrate: `eth_scalper/out_eth_market_chart_30d.json`, `out_btc_market_chart_30d.json`, `eth_scalper/backtest_results.json`, `eth_scalper/reversal_analysis.json`, and related droplet copies.
- ETH-derived reversal parameter family, when ported as a BTC harness template, produced the first positive-shaped candidate (~+0.53% total net, 2 trades, 100% win rate), suggesting the ETH reversal structure is a better BTC-only base than the earlier live BTC-only bridge heuristics.
- User explicitly approved adopting ETH-derived reversal logic for BTC live trading (BTC/USDC only). During live cutover, inherited stale hold state (`hold≈2534`) caused immediate BTC->USDC exit behavior; durable lesson: when swapping strategy families live, reset/normalize inherited hold-state before first evaluation.
- User was frustrated by repeated confirmations and partial completion; durable expectation is to execute approved changes end-to-end without making them restate the same instruction repeatedly.
