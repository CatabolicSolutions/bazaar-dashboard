- Bazaar/Bloc durable update: War Room dashboard truth layer was repaired so stale Tradier execution rejects no longer masquerade as live blockers. Snapshot freshness gating now marks stale execution state as stale rather than current truth.
- Bazaar/Tradier durable update: hardened auto-trade and autonomous trader paths so out-of-mandate candidates are blocked earlier, and dashboard snapshot / decision-context logging no longer crashes on permission issues.
- Bazaar/Bloc strategy directive established by Conor Ross: canonical live direction is ETH/BTC all-in compounding volatility capture. Required live parameters: min trigger 0.10%, min net target after friction 0.20%, max hold 180s, max daily trades 30, min signal score 4/10, hard stop 0.10%, trade only when expected edge exceeds friction; prefer pullback/reversion entries below midpoint, require liquidity, recycle realized gains, and log blockers immediately.
- Bazaar/Bloc durable implementation progress: dashboard capital truth now reflects estimated total capital instead of only USDC; live strategy label set to compounding_volatility_capture.
- Bazaar/Bloc durable operational lesson: eth-scalper service repeatedly failed on module import/path assumptions under systemd (`python3 -m bot.main` with WorkingDirectory/PYTHONPATH rooted at `/var/www/bazaar/eth_scalper`). Boot path was eventually stabilized by removing brittle execution_lifecycle import dependency from bot startup.
- Bazaar/Bloc current verified runtime state as of 2026-04-18 late afternoon: eth-scalper service boots and writes fresh state again; live logs showed current wallet around 0.0003 ETH and 177.53 USDC, with existing WETH exposure (~$75.46 inventory) blocking fresh entries because no resumable bound lot existed.
- Bazaar/Bloc next durable fix committed: orphan WETH should be promoted into managed all-in compounding inventory instead of acting as a hard blocker. Commit pushed: `1959aa2` (`Promote orphan WETH into managed all-in compounding inventory`). User still needs to deploy/restart on VPS after this last patch.
- Bazaar/Bloc durable update: live ETH scalper path now proves natural ETH midline detection and quote qualification are working on VPS. Multiple live logs showed `at_or_below_mid=True`, `Qualified for execution`, wallet near `0.0003 ETH, $252.89 USDC`, and expected edge around `0.0106%` to `0.0250%`.
- Bazaar/Bloc durable blocker isolated: execution was failing before swap submission with exact error `AttributeError("'LiveExecutor' object has no attribute 'pretrade_invariant_check'")` from `/var/www/bazaar/eth_scalper/bot/main.py` in `ETHScalper._execute_live_trade`. This established the active blocker as executor compatibility, not signal generation, pricing, or capital sizing.
- Bazaar/Bloc compatibility patches created and pushed on 2026-04-18: `c792a9e` (`Fallback when live executor lacks invariant check`) and `0405ae9` (`Handle broken executor attribute lookup safely`). Durable requirement: `_execute_live_trade` must not rely on plain `hasattr(...)` against this deployed `LiveExecutor`; attribute lookup itself may raise and must be wrapped safely, then proceed down direct swap path when invariant check is unavailable.
- Bazaar/Bloc deployment verification lesson: journal output can include stale pre-restart lines that still show old traceback paths. After restart, fresh process `python3[1759178]` confirmed the old crash lines belonged to prior process `python3[1758539]`. Post-restart runtime showed no immediate invariant-check crash but instead fallback behavior: `No natural signal recently, forcing fallback live entry` followed by `Manual fallback skipped: insufficient real move (+0.0000% / 60s)`.
- Bazaar/Bloc anti-spam safeguard added: duplicate synthetic position creation during repeated execution attempts was identified as a real operational risk while the executor path remains unresolved. Patch committed: `49a1d7f` (`Prevent duplicate Bloc entries while execution is pending`). User still needed to deploy/restart after this last patch.
- 2026-04-18 evening: ETH Scalper boot under `eth-scalper.service` was restored after repeated `ModuleNotFoundError` crashes around `db.client` import handling. Live startup now reaches banner and canonical state-path logs (`/var/www/bazaar/eth_scalper/state`).
- Durable lesson: optional DB logging must never be a fatal boot dependency for the live bot. Systemd/runtime import assumptions around `db` and `eth_scalper.db` were brittle.
- Confirmed canonical strategy intent from Conor Ross: keep the **All-In Compounding** protocol as authoritative. Deployed non-USDC inventory is active capital, not missing capital.
- Verified live wallet truth from user-provided screenshot/logs: account was holding about `$250.41` in `0.00331 CBBTC` while bot/runtime still reported `Available Capital: $0.00` and emitted `Insufficient capital` checks. This was identified as a truth/reporting bug, not a strategy change request.
- Direction clarified by user: stop reworking the scalper’s core protocol beyond correctness fixes; focus on making HQ/Dashboard/War Room genuinely useful and PostgreSQL-backed while preserving compounding behavior.
- Follow-up patch direction recorded: runtime and snapshot/War Room should distinguish deployable USDC vs invested inventory, surface held asset/value/status, and represent inventory-backed holding state as active compounding position truth rather than flat/empty state.
