# 2026-03-18

- User challenged Kalshi price-first framing and pushed toward treating prediction markets in their native language: probabilities.
- Alfred agreed the correct architecture is probability-first rather than quote-first.
- Formal green light given by user to begin the probability-first Kalshi refactor.
- Refactor direction to preserve across restart/session loss:
  - contract discovery layer
  - contract interpretation / normalization layer
  - implied probability extraction
  - fair probability estimation layer
  - edge scoring and confidence layer
  - execution validation as the last gate, not the first gate
- Key lesson locked in: shallow Kalshi market-list metadata should not be mistaken for the full economic/price reality of the contract; analysis must be restructured accordingly.
- Concrete Kalshi architecture breakthrough achieved:
  - raw trade API endpoints expose fields like `yes_bid_dollars`, `yes_ask_dollars`, `no_bid_dollars`, `no_ask_dollars`, `last_price_dollars`, and multivariate leg details
  - SDK layer was masking/misvalidating some live markets, so Kalshi analysis should use raw HTTP responses where needed
- First raw-API probability-first pass was implemented:
  - processor now uses raw `/markets` payloads for implied-probability extraction
  - first pass still returned `No Trade`, with rejection reasons shifting to `too_far_to_resolution` and `reject_hard`
  - this is progress because the engine is now rejecting on time-to-resolution and structural complexity rather than just missing shallow quote fields
