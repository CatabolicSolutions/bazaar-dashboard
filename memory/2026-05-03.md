# 2026-05-03

## Bot R11 Tuning Applied
- STOP_LOSS 0.15→0.25 (wider stop), VOL_FLOOR 0.12→0.15, VOL_CAP 0.20→0.25 (tighter band), REENTRY_SCORE_THRESHOLD 0.50→0.45 (lower reentry bar)
- ARM_WAIT_ALLOWED=0 added (gates arm_wait entry class, toggleable without redeploy)
- recovery_mode scoping bug fixed (local var read before assignment)
- R11_TUNING_RECORD.md created with full config, recovery instructions

## Dashboard Fix
- Patched `/var/www/bazaar/dashboard/scripts/serve_dashboard.py` to read fresh from `scalp_state.json` + `tick_deque.json` instead of stale `eth_scalper_status.json` (last written May 2 21:54 UTC, bot was restarted since)
- Added `entry_target_price` to API response (ema12×trigger formula)
- Frontend at `/var/www/bazaar/dashboard/public/bloc/index.html` updated with 4 stat pills and bot config panel

## Forced Manual Entry (USDC→WETH)
- Executor IS live (confirmed nonces 138, 139, 140 on Base chain 8453, wallet 0xff9cc57be71a7b851a422a851600ac23b3843e27)
- First swap (nonce 138) worked (0.1068 WETH bought for $246.18 USDC) but bot sold it immediately via stale-loss exit because last_flip timestamp was 4.8h old (state write failed — permission error)
- Second swap (nonce 140) + sudo state write succeeded — bot now holds WETH at ~$2,303.53, streak=0/1, no sell signals

## Critical Discoveries
- **WETH balanceOf bug**: RPC eth_call for balanceOf at WETH address `0x4200000000000000000000000000000000000006` returns 0 despite on-chain Transfer events showing WETH received. Bot's own `get_balances()` reads correctly (contract ABI mismatch). Use bot's balance function or contract object.
- **State file ownership**: `/var/www/bazaar/eth_scalper/state/scalp_state.json` owned by root. `sudo python3 -c` required for writes. alfred-deploy has sudo for python3.
- **Stale-loss exit logic**: Bot will sell 4.8h+ held positions with negative extension. COOLDOWN_SEC=30s between flips.
- **Bot tick interval**: ~62s between loop iterations. USDC/WETH detection via on-chain balances, validated against state file.

## Bot Operations Verified
- Executor enabled (live.trading, not paper mode)
- Uniswap V3 routing via Universal Router (0x2626664c...)
- Swap path: USDC→pool(0xd0b53...)→WETH
- Bot handles inventory reconciliation (detected WETH from our manual swap)
- Bot PID 712226, same process throughout (restarted 05:14 UTC with R11 config)
- Dashboard API at `127.0.0.1:8765/api/hq/status` returns correct live data
# 2026-05-03

## Bot R11 Tuning Applied
- STOP_LOSS 0.15→0.25 (wider stop), VOL_FLOOR 0.12→0.15, VOL_CAP 0.20→0.25 (tighter band), REENTRY_SCORE_THRESHOLD 0.50→0.45 (lower reentry bar)
- ARM_WAIT_ALLOWED=0 added (gates arm_wait entry class, toggleable without redeploy)
- recovery_mode scoping bug fixed (local var read before assignment)
- R11_TUNING_RECORD.md created with full config, recovery instructions

## Dashboard Fix
- Patched `/var/www/bazaar/dashboard/scripts/serve_dashboard.py` to read fresh from `scalp_state.json` + `tick_deque.json` instead of stale `eth_scalper_status.json` (last written May 2 21:54 UTC, bot was restarted since)
- Added `entry_target_price` to API response (ema12×trigger formula)
- Frontend at `/var/www/bazaar/dashboard/public/bloc/index.html` updated with 4 stat pills and bot config panel

## Forced Manual Entry (USDC→WETH)
- Executor IS live (confirmed nonces 138, 139, 140 on Base chain 8453, wallet 0xff9cc57be71a7b851a422a851600ac23b3843e27)
- First swap (nonce 138) worked (0.1068 WETH bought for $246.18 USDC) but bot sold it immediately via stale-loss exit because last_flip timestamp was 4.8h old (state write failed — permission error)
- Second swap (nonce 140) + sudo state write succeeded — bot now holds WETH at ~$2,303.53, streak=0/1, no sell signals

## Critical Discoveries
- **WETH balanceOf bug**: RPC eth_call for balanceOf at WETH address `0x4200000000000000000000000000000000000006` returns 0 despite on-chain Transfer events showing WETH received. Bot's own `get_balances()` reads correctly (contract ABI mismatch). Use bot's balance function or contract object.
- **State file ownership**: `/var/www/bazaar/eth_scalper/state/scalp_state.json` owned by root. `sudo python3 -c` required for writes. alfred-deploy has sudo for python3.
- **Stale-loss exit logic**: Bot will sell 4.8h+ held positions with negative extension. COOLDOWN_SEC=30s between flips.
- **Bot tick interval**: ~62s between loop iterations. USDC/WETH detection via on-chain balances, validated against state file.

## Bot Operations Verified
- Executor enabled (live.trading, not paper mode)
- Uniswap V3 routing via Universal Router (0x2626664c...)
- Swap path: USDC→pool(0xd0b53...)→WETH
- Bot handles inventory reconciliation (detected WETH from our manual swap)
- Bot PID 712226, same process throughout (restarted 05:14 UTC with R11 config)
- Dashboard API at `127.0.0.1:8765/api/hq/status` returns correct live data

## Phase 1 Frontend Analytics Lift (2026-04-14 00:57-01:16 MT)
- Four new backend endpoints injected into `serve_dashboard.py`:
  - `GET /api/hq/cycle-ledger` → reads `eth_scalper_cycle_ledger.jsonl` (41 trades)
  - `GET /api/hq/cycle-scorecard` → reads `eth_scalper_cycle_scorecard.json` (4 cycles, 1W/3L, net PnL -$0.0118)
  - `GET /api/hq/execution-feed` → combines cycle ledger + `tick_deque.json` (30 entries)
  - `GET /api/hq/trade-history` → aggregates ledger + scorecard + realized + scalp_state
- Frontend rewritten at `/var/www/bazaar/dashboard/public/bloc/index.html` with:
  - **Trade history table** — scrollable, color-coded PnL (green/red), entry class, side flips, WETH delta, tx hash
  - **Cycle PnL breakdown bar chart** — per-cycle bar showing net PnL on `cycleChart` canvas
  - **Execution feed** — scrolling chronological log of bot actions, auto-updates every 15s
  - **Performance stats panel** — win rate, avg PnL, net PnL, net WETH Δ, forward baseline, trade count
  - **Enhanced price chart annotations** — green/red dots for entry/exit markers, continuation/rollover shading on price chart
  - **Correct field mapping** against real `scalp_pool` API keys (sp.entry, sp.pnl, sp.weth, sp.trades, sp.value_usd, sp.preferred_reentry_price, sp.weth_edge_pct)
- All existing panels preserved: stat pills, bot config, parameter controls, strategy lab, levels, position truth, accumulation math
- Verified: all endpoints return valid data, backend syntax passes py_compile, dashboard service restarted successfully

## Frontend Card System + Timezone Fix (2026-05-03 01:16 MT)
- Panels converted to **5 horizontal summary cards**: Performance, Position, Execution, Config (R11), Accumulation
- Each card shows a compact key metric (win rate %, side, stop loss, edge %, etc.) on the summary face
- Click a card to drill into its expanded detail; only one card open at a time (collapsible accordion style)
- Performance expanded: perf stats + cycle bar chart + full trade history table
- Position expanded: position truth + prime levels + visual key
- Execution expanded: execution timeline chart + scrolling exec feed
- Config expanded: full R11 bot config + parameter controls with save/apply
- Accumulation expanded: accumulation math + cycle scorecard detail with last cycle breakdown
- **All timestamps converted to America/Denver (Mountain Time)** via JS Intl API — applies to trade history table, execution feed, cycle chart labels, price chart x-axis, and summary/status line
- Hero price chart + stat pills + strategy lab remain as top-level always-visible elements
- API endpoints unchanged; all data sourced from live scalp_pool, cycle ledger, and tick_deque

## Frontend Bug Fixes (2026-05-03 01:41-01:55 MT)
- **Range buttons (1h/4h/8h/12h) fixed**: `load()` was not using the `range` variable — always fetched full 720-point dataset regardless of button selection. Fixed by passing `?limit=range+480` to API and `.slice(-range)` on returned data.
- **Signal/bands chart restored**: `signalChart` canvas was deleted during card refactor. Added back as dedicated right panel in chart row showing WETH edge pct (purple), trigger level (gold dashed), zero line reference.
- **Execution timeline drawn properly**: Custom fill-area logic was broken (nested braces caused regex replacement to fail, leaving orphaned `for` loop after function closing brace). Replaced with clean `drawLines`-style approach drawing step lines at y=0.4 (inactive) / y=1.2 (active) for continuation_hold and rollover_ready states, with "active"/"inactive" axis labels.
- **Orphaned code fixed**: Cleanup script removed `// Fill under continuation=1.2` block but left a standalone `for(let i=0;i<5;i++)...` loop after `drawExecChart()`'s close, causing JS syntax error that zeroed all dashboard data. Surgically removed the orphaned loop. All 4 canvas functions working correctly.
- All chart functions verified clean: drawPriceChart, drawSignalChart, drawExecChart, drawCycleChart. API at /api/hq/status returns live data (USDC side, 53 trades, $2.77 PnL).
# 2026-05-03

## Bot R11 Tuning Applied
- STOP_LOSS 0.15→0.25 (wider stop), VOL_FLOOR 0.12→0.15, VOL_CAP 0.20→0.25 (tighter band), REENTRY_SCORE_THRESHOLD 0.50→0.45 (lower reentry bar)
- ARM_WAIT_ALLOWED=0 added (gates arm_wait entry class, toggleable without redeploy)
- recovery_mode scoping bug fixed (local var read before assignment)
- R11_TUNING_RECORD.md created with full config, recovery instructions

## Dashboard Fix
- Patched `/var/www/bazaar/dashboard/scripts/serve_dashboard.py` to read fresh from `scalp_state.json` + `tick_deque.json` instead of stale `eth_scalper_status.json` (last written May 2 21:54 UTC, bot was restarted since)
- Added `entry_target_price` to API response (ema12×trigger formula)
- Frontend at `/var/www/bazaar/dashboard/public/bloc/index.html` updated with 4 stat pills and bot config panel

## Forced Manual Entry (USDC→WETH)
- Executor IS live (confirmed nonces 138, 139, 140 on Base chain 8453, wallet 0xff9cc57be71a7b851a422a851600ac23b3843e27)
- First swap (nonce 138) worked (0.1068 WETH bought for $246.18 USDC) but bot sold it immediately via stale-loss exit because last_flip timestamp was 4.8h old (state write failed — permission error)
- Second swap (nonce 140) + sudo state write succeeded — bot now holds WETH at ~$2,303.53, streak=0/1, no sell signals

## Critical Discoveries
- **WETH balanceOf bug**: RPC eth_call for balanceOf at WETH address `0x4200000000000000000000000000000000000006` returns 0 despite on-chain Transfer events showing WETH received. Bot's own `get_balances()` reads correctly (contract ABI mismatch). Use bot's balance function or contract object.
- **State file ownership**: `/var/www/bazaar/eth_scalper/state/scalp_state.json` owned by root. `sudo python3 -c` required for writes. alfred-deploy has sudo for python3.
- **Stale-loss exit logic**: Bot will sell 4.8h+ held positions with negative extension. COOLDOWN_SEC=30s between flips.
- **Bot tick interval**: ~62s between loop iterations. USDC/WETH detection via on-chain balances, validated against state file.

## Bot Operations Verified
- Executor enabled (live.trading, not paper mode)
- Uniswap V3 routing via Universal Router (0x2626664c...)
- Swap path: USDC→pool(0xd0b53...)→WETH
- Bot handles inventory reconciliation (detected WETH from our manual swap)
- Bot PID 712226, same process throughout (restarted 05:14 UTC with R11 config)
- Dashboard API at `127.0.0.1:8765/api/hq/status` returns correct live data

## Phase 1 Frontend Analytics Lift (2026-04-14 00:57-01:16 MT)
- Four new backend endpoints injected into `serve_dashboard.py`:
  - `GET /api/hq/cycle-ledger` → reads `eth_scalper_cycle_ledger.jsonl` (41 trades)
  - `GET /api/hq/cycle-scorecard` → reads `eth_scalper_cycle_scorecard.json` (4 cycles, 1W/3L, net PnL -$0.0118)
  - `GET /api/hq/execution-feed` → combines cycle ledger + `tick_deque.json` (30 entries)
  - `GET /api/hq/trade-history` → aggregates ledger + scorecard + realized + scalp_state
- Frontend rewritten at `/var/www/bazaar/dashboard/public/bloc/index.html` with:
  - **Trade history table** — scrollable, color-coded PnL (green/red), entry class, side flips, WETH delta, tx hash
  - **Cycle PnL breakdown bar chart** — per-cycle bar showing net PnL on `cycleChart` canvas
  - **Execution feed** — scrolling chronological log of bot actions, auto-updates every 15s
  - **Performance stats panel** — win rate, avg PnL, net PnL, net WETH Δ, forward baseline, trade count
  - **Enhanced price chart annotations** — green/red dots for entry/exit markers, continuation/rollover shading on price chart
  - **Correct field mapping** against real `scalp_pool` API keys (sp.entry, sp.pnl, sp.weth, sp.trades, sp.value_usd, sp.preferred_reentry_price, sp.weth_edge_pct)
- All existing panels preserved: stat pills, bot config, parameter controls, strategy lab, levels, position truth, accumulation math
- Verified: all endpoints return valid data, backend syntax passes py_compile, dashboard service restarted successfully

## Frontend Card System + Timezone Fix (2026-05-03 01:16 MT)
- Panels converted to **5 horizontal summary cards**: Performance, Position, Execution, Config (R11), Accumulation
- Each card shows a compact key metric (win rate %, side, stop loss, edge %, etc.) on the summary face
- Click a card to drill into its expanded detail; only one card open at a time (collapsible accordion style)
- Performance expanded: perf stats + cycle bar chart + full trade history table
- Position expanded: position truth + prime levels + visual key
- Execution expanded: execution timeline chart + scrolling exec feed
- Config expanded: full R11 bot config + parameter controls with save/apply
- Accumulation expanded: accumulation math + cycle scorecard detail with last cycle breakdown
- **All timestamps converted to America/Denver (Mountain Time)** via JS Intl API — applies to trade history table, execution feed, cycle chart labels, price chart x-axis, and summary/status line
- Hero price chart + stat pills + strategy lab remain as top-level always-visible elements
- API endpoints unchanged; all data sourced from live scalp_pool, cycle ledger, and tick_deque

## Frontend Bug Fixes (2026-05-03 01:41-01:55 MT)
- **Range buttons (1h/4h/8h/12h) fixed**: `load()` was not using the `range` variable — always fetched full 720-point dataset regardless of button selection. Fixed by passing `?limit=range+480` to API and `.slice(-range)` on returned data.
- **Signal/bands chart restored**: `signalChart` canvas was deleted during card refactor. Added back as dedicated right panel in chart row showing WETH edge pct (purple), trigger level (gold dashed), zero line reference.
- **Execution timeline drawn properly**: Custom fill-area logic was broken (nested braces caused regex replacement to fail, leaving orphaned `for` loop after function closing brace). Replaced with clean `drawLines`-style approach drawing step lines at y=0.4 (inactive) / y=1.2 (active) for continuation_hold and rollover_ready states, with "active"/"inactive" axis labels.
- **Orphaned code fixed**: Cleanup script removed `// Fill under continuation=1.2` block but left a standalone `for(let i=0;i<5;i++)...` loop after `drawExecChart()`'s close, causing JS syntax error that zeroed all dashboard data. Surgically removed the orphaned loop. All 4 canvas functions working correctly.
- All chart functions verified clean: drawPriceChart, drawSignalChart, drawExecChart, drawCycleChart. API at /api/hq/status returns live data (USDC side, 53 trades, $2.77 PnL).

## Phase 2 Visual Upgrade (2026-05-03 01:59-02:03 MT)
- **2.1 Chart crosshair/tooltip** delivered: mousemove on price chart draws dashed crosshair lines and pops up `#chartTip` div with exact price, time, distances to prime buy/sell/entry, WETH edge %. Trade marker detection within 20px proximity shows entry/exit class, PnL, side flip, tx hash. Tooltip auto-flips if near viewport edges; mouseleave clears and restores chart.
- **2.2 Mini sparklines**: All 8 stat pills (Wallet, WETH, PnL%, Accum edge, Stop loss, Vol band, Reentry, ARM wait) now have inline sparkline canvases (240×44 actual, 22px CSS). Fed from last 60 price ticks. Edge spark shows WETH edge % (purple), Vol band shows prime_sell−prime_buy spread (amber), Stop loss plots constant 0.25% (red). Refresh every 30s.
- **2.3 Layout restructured**: Abandoned card/drill-down system for fixed zone layout — chart row (2.2fr/1fr), mid-row 3-column (cycle breakdown / trade table / execution feed), bottom row 2-column (bot config / parameter controls). Strategy lab toggleable via button. Mobile responsive breakpoints at 1100/768/480px.
- **2.4 Color & typography**: Dark theme with radial-gradient background, 3D depth, glow effects. h1 uppercase with negative tracking, smallcaps h2/h3. Semantic color system (good=#20d29a, warn=#ffb84d, bad=#ff5d73, blue=#7ec8ff, gold=#f4c542). Status chips with glow-good/glow-blue/glow-warn box-shadow classes. All 26 feature checks pass. Deployed as single-file HTML (37KB) to `/var/www/bazaar/dashboard/public/bloc/index.html`.

## Phase 3 Forecast & Analytics (2026-05-03 02:14-02:23 MT)
- **3.1 EMA Projection**: 4-item forecast row (EMA12, EMA50, Delta/Velocity, Convergence) computed from tick data. EMA12-EMA50 delta computed over 20-tick window. When delta velocity < 0 (converging): projects cross tick count and crossing price. Updates every 15s.
- **3.2 Performance Dashboard**: Dedicated panel with win rate (wins/total×100), avg PnL/cycle, Sharpe-like ratio (avg/stdev×√n), max consecutive wins/losses. All computed client-side from trade ledger. Color-coded (green when >0, red when <0).
- **3.3 Strategy Lab Evolution**: 8 what-if slider controls (Distance, Pullback, Reversal, Target%, Stop%, Trail%, Max Hold, Friction) with linked text inputs. Client-side simulation engine using 726-point 30d market data: SMA crossover + volume filter entry, target/stop/maxhold exit rules. Results: trade count, win rate, avg $ per trade. Trade overlay toggle shows both simulated (green/red dots on lab chart) and actual bot trades on 30d price chart. Built-in R11/R12 preset dropdown + save/load to localStorage. Full single-file deployment (43.5KB), 30/30 feature checks pass.
# 2026-05-03

## Bot R11 Tuning Applied
- STOP_LOSS 0.15→0.25 (wider stop), VOL_FLOOR 0.12→0.15, VOL_CAP 0.20→0.25 (tighter band), REENTRY_SCORE_THRESHOLD 0.50→0.45 (lower reentry bar)
- ARM_WAIT_ALLOWED=0 added (gates arm_wait entry class, toggleable without redeploy)
- recovery_mode scoping bug fixed (local var read before assignment)
- R11_TUNING_RECORD.md created with full config, recovery instructions

## Dashboard Fix
- Patched `/var/www/bazaar/dashboard/scripts/serve_dashboard.py` to read fresh from `scalp_state.json` + `tick_deque.json` instead of stale `eth_scalper_status.json` (last written May 2 21:54 UTC, bot was restarted since)
- Added `entry_target_price` to API response (ema12×trigger formula)
- Frontend at `/var/www/bazaar/dashboard/public/bloc/index.html` updated with 4 stat pills and bot config panel

## Forced Manual Entry (USDC→WETH)
- Executor IS live (confirmed nonces 138, 139, 140 on Base chain 8453, wallet 0xff9cc57be71a7b851a422a851600ac23b3843e27)
- First swap (nonce 138) worked (0.1068 WETH bought for $246.18 USDC) but bot sold it immediately via stale-loss exit because last_flip timestamp was 4.8h old (state write failed — permission error)
- Second swap (nonce 140) + sudo state write succeeded — bot now holds WETH at ~$2,303.53, streak=0/1, no sell signals

## Critical Discoveries
- **WETH balanceOf bug**: RPC eth_call for balanceOf at WETH address `0x4200000000000000000000000000000000000006` returns 0 despite on-chain Transfer events showing WETH received. Bot's own `get_balances()` reads correctly (contract ABI mismatch). Use bot's balance function or contract object.
- **State file ownership**: `/var/www/bazaar/eth_scalper/state/scalp_state.json` owned by root. `sudo python3 -c` required for writes. alfred-deploy has sudo for python3.
- **Stale-loss exit logic**: Bot will sell 4.8h+ held positions with negative extension. COOLDOWN_SEC=30s between flips.
- **Bot tick interval**: ~62s between loop iterations. USDC/WETH detection via on-chain balances, validated against state file.

## Bot Operations Verified
- Executor enabled (live.trading, not paper mode)
- Uniswap V3 routing via Universal Router (0x2626664c...)
- Swap path: USDC→pool(0xd0b53...)→WETH
- Bot handles inventory reconciliation (detected WETH from our manual swap)
- Bot PID 712226, same process throughout (restarted 05:14 UTC with R11 config)
- Dashboard API at `127.0.0.1:8765/api/hq/status` returns correct live data

## Phase 1 Frontend Analytics Lift (2026-04-14 00:57-01:16 MT)
- Four new backend endpoints injected into `serve_dashboard.py`:
  - `GET /api/hq/cycle-ledger` → reads `eth_scalper_cycle_ledger.jsonl` (41 trades)
  - `GET /api/hq/cycle-scorecard` → reads `eth_scalper_cycle_scorecard.json` (4 cycles, 1W/3L, net PnL -$0.0118)
  - `GET /api/hq/execution-feed` → combines cycle ledger + `tick_deque.json` (30 entries)
  - `GET /api/hq/trade-history` → aggregates ledger + scorecard + realized + scalp_state
- Frontend rewritten at `/var/www/bazaar/dashboard/public/bloc/index.html` with:
  - **Trade history table** — scrollable, color-coded PnL (green/red), entry class, side flips, WETH delta, tx hash
  - **Cycle PnL breakdown bar chart** — per-cycle bar showing net PnL on `cycleChart` canvas
  - **Execution feed** — scrolling chronological log of bot actions, auto-updates every 15s
  - **Performance stats panel** — win rate, avg PnL, net PnL, net WETH Δ, forward baseline, trade count
  - **Enhanced price chart annotations** — green/red dots for entry/exit markers, continuation/rollover shading on price chart
  - **Correct field mapping** against real `scalp_pool` API keys (sp.entry, sp.pnl, sp.weth, sp.trades, sp.value_usd, sp.preferred_reentry_price, sp.weth_edge_pct)
- All existing panels preserved: stat pills, bot config, parameter controls, strategy lab, levels, position truth, accumulation math
- Verified: all endpoints return valid data, backend syntax passes py_compile, dashboard service restarted successfully

## Frontend Card System + Timezone Fix (2026-05-03 01:16 MT)
- Panels converted to **5 horizontal summary cards**: Performance, Position, Execution, Config (R11), Accumulation
- Each card shows a compact key metric (win rate %, side, stop loss, edge %, etc.) on the summary face
- Click a card to drill into its expanded detail; only one card open at a time (collapsible accordion style)
- Performance expanded: perf stats + cycle bar chart + full trade history table
- Position expanded: position truth + prime levels + visual key
- Execution expanded: execution timeline chart + scrolling exec feed
- Config expanded: full R11 bot config + parameter controls with save/apply
- Accumulation expanded: accumulation math + cycle scorecard detail with last cycle breakdown
- **All timestamps converted to America/Denver (Mountain Time)** via JS Intl API — applies to trade history table, execution feed, cycle chart labels, price chart x-axis, and summary/status line
- Hero price chart + stat pills + strategy lab remain as top-level always-visible elements
- API endpoints unchanged; all data sourced from live scalp_pool, cycle ledger, and tick_deque

## Frontend Bug Fixes (2026-05-03 01:41-01:55 MT)
- **Range buttons (1h/4h/8h/12h) fixed**: `load()` was not using the `range` variable — always fetched full 720-point dataset regardless of button selection. Fixed by passing `?limit=range+480` to API and `.slice(-range)` on returned data.
- **Signal/bands chart restored**: `signalChart` canvas was deleted during card refactor. Added back as dedicated right panel in chart row showing WETH edge pct (purple), trigger level (gold dashed), zero line reference.
- **Execution timeline drawn properly**: Custom fill-area logic was broken (nested braces caused regex replacement to fail, leaving orphaned `for` loop after function closing brace). Replaced with clean `drawLines`-style approach drawing step lines at y=0.4 (inactive) / y=1.2 (active) for continuation_hold and rollover_ready states, with "active"/"inactive" axis labels.
- **Orphaned code fixed**: Cleanup script removed `// Fill under continuation=1.2` block but left a standalone `for(let i=0;i<5;i++)...` loop after `drawExecChart()`'s close, causing JS syntax error that zeroed all dashboard data. Surgically removed the orphaned loop. All 4 canvas functions working correctly.
- All chart functions verified clean: drawPriceChart, drawSignalChart, drawExecChart, drawCycleChart. API at /api/hq/status returns live data (USDC side, 53 trades, $2.77 PnL).

## Phase 2 Visual Upgrade (2026-05-03 01:59-02:03 MT)
- **2.1 Chart crosshair/tooltip** delivered: mousemove on price chart draws dashed crosshair lines and pops up `#chartTip` div with exact price, time, distances to prime buy/sell/entry, WETH edge %. Trade marker detection within 20px proximity shows entry/exit class, PnL, side flip, tx hash. Tooltip auto-flips if near viewport edges; mouseleave clears and restores chart.
- **2.2 Mini sparklines**: All 8 stat pills (Wallet, WETH, PnL%, Accum edge, Stop loss, Vol band, Reentry, ARM wait) now have inline sparkline canvases (240×44 actual, 22px CSS). Fed from last 60 price ticks. Edge spark shows WETH edge % (purple), Vol band shows prime_sell−prime_buy spread (amber), Stop loss plots constant 0.25% (red). Refresh every 30s.
- **2.3 Layout restructured**: Abandoned card/drill-down system for fixed zone layout — chart row (2.2fr/1fr), mid-row 3-column (cycle breakdown / trade table / execution feed), bottom row 2-column (bot config / parameter controls). Strategy lab toggleable via button. Mobile responsive breakpoints at 1100/768/480px.
- **2.4 Color & typography**: Dark theme with radial-gradient background, 3D depth, glow effects. h1 uppercase with negative tracking, smallcaps h2/h3. Semantic color system (good=#20d29a, warn=#ffb84d, bad=#ff5d73, blue=#7ec8ff, gold=#f4c542). Status chips with glow-good/glow-blue/glow-warn box-shadow classes. All 26 feature checks pass. Deployed as single-file HTML (37KB) to `/var/www/bazaar/dashboard/public/bloc/index.html`.

## Phase 3 Forecast & Analytics (2026-05-03 02:14-02:23 MT)
- **3.1 EMA Projection**: 4-item forecast row (EMA12, EMA50, Delta/Velocity, Convergence) computed from tick data. EMA12-EMA50 delta computed over 20-tick window. When delta velocity < 0 (converging): projects cross tick count and crossing price. Updates every 15s.
- **3.2 Performance Dashboard**: Dedicated panel with win rate (wins/total×100), avg PnL/cycle, Sharpe-like ratio (avg/stdev×√n), max consecutive wins/losses. All computed client-side from trade ledger. Color-coded (green when >0, red when <0).
- **3.3 Strategy Lab Evolution**: 8 what-if slider controls (Distance, Pullback, Reversal, Target%, Stop%, Trail%, Max Hold, Friction) with linked text inputs. Client-side simulation engine using 726-point 30d market data: SMA crossover + volume filter entry, target/stop/maxhold exit rules. Results: trade count, win rate, avg $ per trade. Trade overlay toggle shows both simulated (green/red dots on lab chart) and actual bot trades on 30d price chart. Built-in R11/R12 preset dropdown + save/load to localStorage. Full single-file deployment (43.5KB), 30/30 feature checks pass.

## Phase 3 User Report + Centralized Chart (V4) (2026-05-03 02:25-04:20 MT)
### Phase 3 Invisible
- User reports "not seeing them" — Phase 3 features (forecast, performance dashboard, strategy lab) not showing
- Server diagnosis confirmed ALL Phase 3 HTML elements present, JS functions defined, braces balanced
- Root cause was browser cache — user reluctant to hard refresh
- Eventually confirmed Phase 3 appeared in lab but: only red dots, no line chart, no bands, no live update, needed "show trade overlay" toggle

### V4 Centralized Single-Chart Architecture
- User requested: ALL info (forecast, performance, what-if overlays) rolled into ONE main chart as single operating surface
- Strategy lab must auto-run on slider change (debounced 300ms)
- One-shot deployment from /tmp/bloc_v4_central.html
- Architecture: removed signalChart canvas, consolidated all overlays into drawMainChart():
  - Legend (6 items) top-left
  - EMA Forecast annotation box top-right (delta, convergence tick/price projection)
  - Performance mini-card top-left below legend (win rate %, avg PnL, Sharpe)
  - WETH edge line on secondary Y-axis (purple, right side)
  - What-if trade overlay as transparent dots on main chart
  - Strategy lab chart (drawLab) kept as reference with price line
  - Auto-whatif on slider change: debounced 300ms
- Deployed to /var/www/bazaar/dashboard/public/bloc/index.html
- Deploy command: scp → sudo python3 shutil.copy2
- Restarted bazaar-dashboard service
- 45289 bytes, 244/244 brace balance

### V4 User Reports Blank Page
- User: "nope... instead everything is blanked out bro" — page completely blank/white
- Diagnosed thoroughly:
  - Confirmed HTML + CSS + JS are syntactically valid (Node.js syntax check passed)
  - Verified nginx serves the correct 45289-byte file with Content-Type: text/html
  - Confirmed all HTML elements present (test page pattern deployed as test.html confirmed working)
  - Confirmed drawSignalChart defined but NEVER called — not the crash source
  - Confirmed all async functions use proper `async function` declaration
  - All verification checks pass (legend found, forecast annotation found, brace balance OK)
- Deployed minimal test page at test.html — user to confirm if visible
- Hypothesis: runtime JS error in V4 version (not syntax) — need user feedback to narrow
- Test page: same path /bloc/test.html, displays "Bloc Bot Terminal" heading and element status

## Decision Log
- Centralized main chart architecture chosen over separate panels per user request
- signalChart canvas removed but function definition preserved (never called)
- Test page deployed as diagnostic step
- Awaiting user confirmation of test page visibility
# 2026-05-03

## Bot R11 Tuning Applied
- STOP_LOSS 0.15→0.25 (wider stop), VOL_FLOOR 0.12→0.15, VOL_CAP 0.20→0.25 (tighter band), REENTRY_SCORE_THRESHOLD 0.50→0.45 (lower reentry bar)
- ARM_WAIT_ALLOWED=0 added (gates arm_wait entry class, toggleable without redeploy)
- recovery_mode scoping bug fixed (local var read before assignment)
- R11_TUNING_RECORD.md created with full config, recovery instructions

## Dashboard Fix
- Patched `/var/www/bazaar/dashboard/scripts/serve_dashboard.py` to read fresh from `scalp_state.json` + `tick_deque.json` instead of stale `eth_scalper_status.json` (last written May 2 21:54 UTC, bot was restarted since)
- Added `entry_target_price` to API response (ema12×trigger formula)
- Frontend at `/var/www/bazaar/dashboard/public/bloc/index.html` updated with 4 stat pills and bot config panel

## Forced Manual Entry (USDC→WETH)
- Executor IS live (confirmed nonces 138, 139, 140 on Base chain 8453, wallet 0xff9cc57be71a7b851a422a851600ac23b3843e27)
- First swap (nonce 138) worked (0.1068 WETH bought for $246.18 USDC) but bot sold it immediately via stale-loss exit because last_flip timestamp was 4.8h old (state write failed — permission error)
- Second swap (nonce 140) + sudo state write succeeded — bot now holds WETH at ~$2,303.53, streak=0/1, no sell signals

## Critical Discoveries
- **WETH balanceOf bug**: RPC eth_call for balanceOf at WETH address `0x4200000000000000000000000000000000000006` returns 0 despite on-chain Transfer events showing WETH received. Bot's own `get_balances()` reads correctly (contract ABI mismatch). Use bot's balance function or contract object.
- **State file ownership**: `/var/www/bazaar/eth_scalper/state/scalp_state.json` owned by root. `sudo python3 -c` required for writes. alfred-deploy has sudo for python3.
- **Stale-loss exit logic**: Bot will sell 4.8h+ held positions with negative extension. COOLDOWN_SEC=30s between flips.
- **Bot tick interval**: ~62s between loop iterations. USDC/WETH detection via on-chain balances, validated against state file.

## Bot Operations Verified
- Executor enabled (live.trading, not paper mode)
- Uniswap V3 routing via Universal Router (0x2626664c...)
- Swap path: USDC→pool(0xd0b53...)→WETH
- Bot handles inventory reconciliation (detected WETH from our manual swap)
- Bot PID 712226, same process throughout (restarted 05:14 UTC with R11 config)
- Dashboard API at `127.0.0.1:8765/api/hq/status` returns correct live data

## Phase 1 Frontend Analytics Lift (2026-04-14 00:57-01:16 MT)
- Four new backend endpoints injected into `serve_dashboard.py`:
  - `GET /api/hq/cycle-ledger` → reads `eth_scalper_cycle_ledger.jsonl` (41 trades)
  - `GET /api/hq/cycle-scorecard` → reads `eth_scalper_cycle_scorecard.json` (4 cycles, 1W/3L, net PnL -$0.0118)
  - `GET /api/hq/execution-feed` → combines cycle ledger + `tick_deque.json` (30 entries)
  - `GET /api/hq/trade-history` → aggregates ledger + scorecard + realized + scalp_state
- Frontend rewritten at `/var/www/bazaar/dashboard/public/bloc/index.html` with:
  - **Trade history table** — scrollable, color-coded PnL (green/red), entry class, side flips, WETH delta, tx hash
  - **Cycle PnL breakdown bar chart** — per-cycle bar showing net PnL on `cycleChart` canvas
  - **Execution feed** — scrolling chronological log of bot actions, auto-updates every 15s
  - **Performance stats panel** — win rate, avg PnL, net PnL, net WETH Δ, forward baseline, trade count
  - **Enhanced price chart annotations** — green/red dots for entry/exit markers, continuation/rollover shading on price chart
  - **Correct field mapping** against real `scalp_pool` API keys (sp.entry, sp.pnl, sp.weth, sp.trades, sp.value_usd, sp.preferred_reentry_price, sp.weth_edge_pct)
- All existing panels preserved: stat pills, bot config, parameter controls, strategy lab, levels, position truth, accumulation math
- Verified: all endpoints return valid data, backend syntax passes py_compile, dashboard service restarted successfully

## Frontend Card System + Timezone Fix (2026-05-03 01:16 MT)
- Panels converted to **5 horizontal summary cards**: Performance, Position, Execution, Config (R11), Accumulation
- Each card shows a compact key metric (win rate %, side, stop loss, edge %, etc.) on the summary face
- Click a card to drill into its expanded detail; only one card open at a time (collapsible accordion style)
- Performance expanded: perf stats + cycle bar chart + full trade history table
- Position expanded: position truth + prime levels + visual key
- Execution expanded: execution timeline chart + scrolling exec feed
- Config expanded: full R11 bot config + parameter controls with save/apply
- Accumulation expanded: accumulation math + cycle scorecard detail with last cycle breakdown
- **All timestamps converted to America/Denver (Mountain Time)** via JS Intl API — applies to trade history table, execution feed, cycle chart labels, price chart x-axis, and summary/status line
- Hero price chart + stat pills + strategy lab remain as top-level always-visible elements
- API endpoints unchanged; all data sourced from live scalp_pool, cycle ledger, and tick_deque

## Frontend Bug Fixes (2026-05-03 01:41-01:55 MT)
- **Range buttons (1h/4h/8h/12h) fixed**: `load()` was not using the `range` variable — always fetched full 720-point dataset regardless of button selection. Fixed by passing `?limit=range+480` to API and `.slice(-range)` on returned data.
- **Signal/bands chart restored**: `signalChart` canvas was deleted during card refactor. Added back as dedicated right panel in chart row showing WETH edge pct (purple), trigger level (gold dashed), zero line reference.
- **Execution timeline drawn properly**: Custom fill-area logic was broken (nested braces caused regex replacement to fail, leaving orphaned `for` loop after function closing brace). Replaced with clean `drawLines`-style approach drawing step lines at y=0.4 (inactive) / y=1.2 (active) for continuation_hold and rollover_ready states, with "active"/"inactive" axis labels.
- **Orphaned code fixed**: Cleanup script removed `// Fill under continuation=1.2` block but left a standalone `for(let i=0;i<5;i++)...` loop after `drawExecChart()`'s close, causing JS syntax error that zeroed all dashboard data. Surgically removed the orphaned loop. All 4 canvas functions working correctly.
- All chart functions verified clean: drawPriceChart, drawSignalChart, drawExecChart, drawCycleChart. API at /api/hq/status returns live data (USDC side, 53 trades, $2.77 PnL).

## Phase 2 Visual Upgrade (2026-05-03 01:59-02:03 MT)
- **2.1 Chart crosshair/tooltip** delivered: mousemove on price chart draws dashed crosshair lines and pops up `#chartTip` div with exact price, time, distances to prime buy/sell/entry, WETH edge %. Trade marker detection within 20px proximity shows entry/exit class, PnL, side flip, tx hash. Tooltip auto-flips if near viewport edges; mouseleave clears and restores chart.
- **2.2 Mini sparklines**: All 8 stat pills (Wallet, WETH, PnL%, Accum edge, Stop loss, Vol band, Reentry, ARM wait) now have inline sparkline canvases (240×44 actual, 22px CSS). Fed from last 60 price ticks. Edge spark shows WETH edge % (purple), Vol band shows prime_sell−prime_buy spread (amber), Stop loss plots constant 0.25% (red). Refresh every 30s.
- **2.3 Layout restructured**: Abandoned card/drill-down system for fixed zone layout — chart row (2.2fr/1fr), mid-row 3-column (cycle breakdown / trade table / execution feed), bottom row 2-column (bot config / parameter controls). Strategy lab toggleable via button. Mobile responsive breakpoints at 1100/768/480px.
- **2.4 Color & typography**: Dark theme with radial-gradient background, 3D depth, glow effects. h1 uppercase with negative tracking, smallcaps h2/h3. Semantic color system (good=#20d29a, warn=#ffb84d, bad=#ff5d73, blue=#7ec8ff, gold=#f4c542). Status chips with glow-good/glow-blue/glow-warn box-shadow classes. All 26 feature checks pass. Deployed as single-file HTML (37KB) to `/var/www/bazaar/dashboard/public/bloc/index.html`.

## Phase 3 Forecast & Analytics (2026-05-03 02:14-02:23 MT)
- **3.1 EMA Projection**: 4-item forecast row (EMA12, EMA50, Delta/Velocity, Convergence) computed from tick data. EMA12-EMA50 delta computed over 20-tick window. When delta velocity < 0 (converging): projects cross tick count and crossing price. Updates every 15s.
- **3.2 Performance Dashboard**: Dedicated panel with win rate (wins/total×100), avg PnL/cycle, Sharpe-like ratio (avg/stdev×√n), max consecutive wins/losses. All computed client-side from trade ledger. Color-coded (green when >0, red when <0).
- **3.3 Strategy Lab Evolution**: 8 what-if slider controls (Distance, Pullback, Reversal, Target%, Stop%, Trail%, Max Hold, Friction) with linked text inputs. Client-side simulation engine using 726-point 30d market data: SMA crossover + volume filter entry, target/stop/maxhold exit rules. Results: trade count, win rate, avg $ per trade. Trade overlay toggle shows both simulated (green/red dots on lab chart) and actual bot trades on 30d price chart. Built-in R11/R12 preset dropdown + save/load to localStorage. Full single-file deployment (43.5KB), 30/30 feature checks pass.

## Phase 3 User Report + Centralized Chart (V4) (2026-05-03 02:25-04:20 MT)
### Phase 3 Invisible
- User reports "not seeing them" — Phase 3 features (forecast, performance dashboard, strategy lab) not showing
- Server diagnosis confirmed ALL Phase 3 HTML elements present, JS functions defined, braces balanced
- Root cause was browser cache — user reluctant to hard refresh
- Eventually confirmed Phase 3 appeared in lab but: only red dots, no line chart, no bands, no live update, needed "show trade overlay" toggle

### V4 Centralized Single-Chart Architecture
- User requested: ALL info (forecast, performance, what-if overlays) rolled into ONE main chart as single operating surface
- Strategy lab must auto-run on slider change (debounced 300ms)
- One-shot deployment from /tmp/bloc_v4_central.html
- Architecture: removed signalChart canvas, consolidated all overlays into drawMainChart():
  - Legend (6 items) top-left
  - EMA Forecast annotation box top-right (delta, convergence tick/price projection)
  - Performance mini-card top-left below legend (win rate %, avg PnL, Sharpe)
  - WETH edge line on secondary Y-axis (purple, right side)
  - What-if trade overlay as transparent dots on main chart
  - Strategy lab chart (drawLab) kept as reference with price line
  - Auto-whatif on slider change: debounced 300ms
- Deployed to /var/www/bazaar/dashboard/public/bloc/index.html
- Deploy command: scp → sudo python3 shutil.copy2
- Restarted bazaar-dashboard service
- 45289 bytes, 244/244 brace balance

### V4 User Reports Blank Page
- User: "nope... instead everything is blanked out bro" — page completely blank/white
- Diagnosed thoroughly:
  - Confirmed HTML + CSS + JS are syntactically valid (Node.js syntax check passed)
  - Verified nginx serves the correct 45289-byte file with Content-Type: text/html
  - Confirmed all HTML elements present (test page pattern deployed as test.html confirmed working)
  - Confirmed drawSignalChart defined but NEVER called — not the crash source
  - Confirmed all async functions use proper `async function` declaration
  - All verification checks pass (legend found, forecast annotation found, brace balance OK)
- Deployed minimal test page at test.html — user to confirm if visible
- Hypothesis: runtime JS error in V4 version (not syntax) — need user feedback to narrow
- Test page: same path /bloc/test.html, displays "Bloc Bot Terminal" heading and element status

## Decision Log
- Centralized main chart architecture chosen over separate panels per user request
- signalChart canvas removed but function definition preserved (never called)
- Test page deployed as diagnostic step
- Awaiting user confirmation of test page visibility

## V4 Recovery & Phase 3 Restoration (2026-05-03 04:42-05:04 MDT)

### Bug 1: Signal Chart + Exec Chart Blank — `pts→p` Replace Overreach
- User confirmed test page works ("I see it") but V4 was blank
- Reverted to Phase 3 working baseline from /tmp/bloc_phase3.html
- Deployed Phase 3 with edits: removed `signalChart` canvas, centralized overlays onto main chart
- User reports: Market Structure/Execution AND Price Bands/Edge Signal panels "both not populating with any data"
- Root cause: Edit 6's blind `pts→p` replace in `drawLines` function hit the parameter name — `drawLines(pts, ...)` uses `pts`, my replace changed it to `p` which was out of scope, breaking ALL canvas drawing (signal chart, exec chart, main chart everything)
- Fixed: reverted bad replace, restored `pts` as parameter in `drawLines`

### Bug 2: Only Main Price Line Visible — Missing `series` Return
- After Bug 1 fix, user reports "just has the main price line. None of the other auxillary/data metric add ons from phases 1 2 and 3"
- Root cause: `drawLines` returns `{px, py, mn, mx, ...}` but NOT `series`. My code in `drawPriceChart` does `info.series.filter(...)` which throws TypeError ("Cannot read properties of undefined") — crashes the entire load chain
- Fixed: added `series` key to `drawLines` return object
- After fix: signal chart, exec chart, trade table, sparklines, all panels render fully
- Phases 1-3 all operational on Phase 3 foundation + centralized chart overlays
- Deployed as V5 to production

### Key Lesson
- When using find-replace across large files, never blindly replace common variables like `pts` or `p` — `pts` is used as a parameter name in `drawLines`, and `p` (point iterator) appears in loops. Always scope replacements to the exact target.
- `drawLines` return object must include `series` array — consuming code assumes `info.series` exists
- Phase 3 foundation (signalChart, execChart preserved) is a safer base than V4's from-scratch rewrite
# 2026-05-03

## Bot R11 Tuning Applied
- STOP_LOSS 0.15→0.25 (wider stop), VOL_FLOOR 0.12→0.15, VOL_CAP 0.20→0.25 (tighter band), REENTRY_SCORE_THRESHOLD 0.50→0.45 (lower reentry bar)
- ARM_WAIT_ALLOWED=0 added (gates arm_wait entry class, toggleable without redeploy)
- recovery_mode scoping bug fixed (local var read before assignment)
- R11_TUNING_RECORD.md created with full config, recovery instructions

## Dashboard Fix
- Patched `/var/www/bazaar/dashboard/scripts/serve_dashboard.py` to read fresh from `scalp_state.json` + `tick_deque.json` instead of stale `eth_scalper_status.json` (last written May 2 21:54 UTC, bot was restarted since)
- Added `entry_target_price` to API response (ema12×trigger formula)
- Frontend at `/var/www/bazaar/dashboard/public/bloc/index.html` updated with 4 stat pills and bot config panel

## Forced Manual Entry (USDC→WETH)
- Executor IS live (confirmed nonces 138, 139, 140 on Base chain 8453, wallet 0xff9cc57be71a7b851a422a851600ac23b3843e27)
- First swap (nonce 138) worked (0.1068 WETH bought for $246.18 USDC) but bot sold it immediately via stale-loss exit because last_flip timestamp was 4.8h old (state write failed — permission error)
- Second swap (nonce 140) + sudo state write succeeded — bot now holds WETH at ~$2,303.53, streak=0/1, no sell signals

## Critical Discoveries
- **WETH balanceOf bug**: RPC eth_call for balanceOf at WETH address `0x4200000000000000000000000000000000000006` returns 0 despite on-chain Transfer events showing WETH received. Bot's own `get_balances()` reads correctly (contract ABI mismatch). Use bot's balance function or contract object.
- **State file ownership**: `/var/www/bazaar/eth_scalper/state/scalp_state.json` owned by root. `sudo python3 -c` required for writes. alfred-deploy has sudo for python3.
- **Stale-loss exit logic**: Bot will sell 4.8h+ held positions with negative extension. COOLDOWN_SEC=30s between flips.
- **Bot tick interval**: ~62s between loop iterations. USDC/WETH detection via on-chain balances, validated against state file.

## Bot Operations Verified
- Executor enabled (live.trading, not paper mode)
- Uniswap V3 routing via Universal Router (0x2626664c...)
- Swap path: USDC→pool(0xd0b53...)→WETH
- Bot handles inventory reconciliation (detected WETH from our manual swap)
- Bot PID 712226, same process throughout (restarted 05:14 UTC with R11 config)
- Dashboard API at `127.0.0.1:8765/api/hq/status` returns correct live data

## Phase 1 Frontend Analytics Lift (2026-04-14 00:57-01:16 MT)
- Four new backend endpoints injected into `serve_dashboard.py`:
  - `GET /api/hq/cycle-ledger` → reads `eth_scalper_cycle_ledger.jsonl` (41 trades)
  - `GET /api/hq/cycle-scorecard` → reads `eth_scalper_cycle_scorecard.json` (4 cycles, 1W/3L, net PnL -$0.0118)
  - `GET /api/hq/execution-feed` → combines cycle ledger + `tick_deque.json` (30 entries)
  - `GET /api/hq/trade-history` → aggregates ledger + scorecard + realized + scalp_state
- Frontend rewritten at `/var/www/bazaar/dashboard/public/bloc/index.html` with:
  - **Trade history table** — scrollable, color-coded PnL (green/red), entry class, side flips, WETH delta, tx hash
  - **Cycle PnL breakdown bar chart** — per-cycle bar showing net PnL on `cycleChart` canvas
  - **Execution feed** — scrolling chronological log of bot actions, auto-updates every 15s
  - **Performance stats panel** — win rate, avg PnL, net PnL, net WETH Δ, forward baseline, trade count
  - **Enhanced price chart annotations** — green/red dots for entry/exit markers, continuation/rollover shading on price chart
  - **Correct field mapping** against real `scalp_pool` API keys (sp.entry, sp.pnl, sp.weth, sp.trades, sp.value_usd, sp.preferred_reentry_price, sp.weth_edge_pct)
- All existing panels preserved: stat pills, bot config, parameter controls, strategy lab, levels, position truth, accumulation math
- Verified: all endpoints return valid data, backend syntax passes py_compile, dashboard service restarted successfully

## Frontend Card System + Timezone Fix (2026-05-03 01:16 MT)
- Panels converted to **5 horizontal summary cards**: Performance, Position, Execution, Config (R11), Accumulation
- Each card shows a compact key metric (win rate %, side, stop loss, edge %, etc.) on the summary face
- Click a card to drill into its expanded detail; only one card open at a time (collapsible accordion style)
- Performance expanded: perf stats + cycle bar chart + full trade history table
- Position expanded: position truth + prime levels + visual key
- Execution expanded: execution timeline chart + scrolling exec feed
- Config expanded: full R11 bot config + parameter controls with save/apply
- Accumulation expanded: accumulation math + cycle scorecard detail with last cycle breakdown
- **All timestamps converted to America/Denver (Mountain Time)** via JS Intl API — applies to trade history table, execution feed, cycle chart labels, price chart x-axis, and summary/status line
- Hero price chart + stat pills + strategy lab remain as top-level always-visible elements
- API endpoints unchanged; all data sourced from live scalp_pool, cycle ledger, and tick_deque

## Frontend Bug Fixes (2026-05-03 01:41-01:55 MT)
- **Range buttons (1h/4h/8h/12h) fixed**: `load()` was not using the `range` variable — always fetched full 720-point dataset regardless of button selection. Fixed by passing `?limit=range+480` to API and `.slice(-range)` on returned data.
- **Signal/bands chart restored**: `signalChart` canvas was deleted during card refactor. Added back as dedicated right panel in chart row showing WETH edge pct (purple), trigger level (gold dashed), zero line reference.
- **Execution timeline drawn properly**: Custom fill-area logic was broken (nested braces caused regex replacement to fail, leaving orphaned `for` loop after function closing brace). Replaced with clean `drawLines`-style approach drawing step lines at y=0.4 (inactive) / y=1.2 (active) for continuation_hold and rollover_ready states, with "active"/"inactive" axis labels.
- **Orphaned code fixed**: Cleanup script removed `// Fill under continuation=1.2` block but left a standalone `for(let i=0;i<5;i++)...` loop after `drawExecChart()`'s close, causing JS syntax error that zeroed all dashboard data. Surgically removed the orphaned loop. All 4 canvas functions working correctly.
- All chart functions verified clean: drawPriceChart, drawSignalChart, drawExecChart, drawCycleChart. API at /api/hq/status returns live data (USDC side, 53 trades, $2.77 PnL).

## Phase 2 Visual Upgrade (2026-05-03 01:59-02:03 MT)
- **2.1 Chart crosshair/tooltip** delivered: mousemove on price chart draws dashed crosshair lines and pops up `#chartTip` div with exact price, time, distances to prime buy/sell/entry, WETH edge %. Trade marker detection within 20px proximity shows entry/exit class, PnL, side flip, tx hash. Tooltip auto-flips if near viewport edges; mouseleave clears and restores chart.
- **2.2 Mini sparklines**: All 8 stat pills (Wallet, WETH, PnL%, Accum edge, Stop loss, Vol band, Reentry, ARM wait) now have inline sparkline canvases (240×44 actual, 22px CSS). Fed from last 60 price ticks. Edge spark shows WETH edge % (purple), Vol band shows prime_sell−prime_buy spread (amber), Stop loss plots constant 0.25% (red). Refresh every 30s.
- **2.3 Layout restructured**: Abandoned card/drill-down system for fixed zone layout — chart row (2.2fr/1fr), mid-row 3-column (cycle breakdown / trade table / execution feed), bottom row 2-column (bot config / parameter controls). Strategy lab toggleable via button. Mobile responsive breakpoints at 1100/768/480px.
- **2.4 Color & typography**: Dark theme with radial-gradient background, 3D depth, glow effects. h1 uppercase with negative tracking, smallcaps h2/h3. Semantic color system (good=#20d29a, warn=#ffb84d, bad=#ff5d73, blue=#7ec8ff, gold=#f4c542). Status chips with glow-good/glow-blue/glow-warn box-shadow classes. All 26 feature checks pass. Deployed as single-file HTML (37KB) to `/var/www/bazaar/dashboard/public/bloc/index.html`.

## Phase 3 Forecast & Analytics (2026-05-03 02:14-02:23 MT)
- **3.1 EMA Projection**: 4-item forecast row (EMA12, EMA50, Delta/Velocity, Convergence) computed from tick data. EMA12-EMA50 delta computed over 20-tick window. When delta velocity < 0 (converging): projects cross tick count and crossing price. Updates every 15s.
- **3.2 Performance Dashboard**: Dedicated panel with win rate (wins/total×100), avg PnL/cycle, Sharpe-like ratio (avg/stdev×√n), max consecutive wins/losses. All computed client-side from trade ledger. Color-coded (green when >0, red when <0).
- **3.3 Strategy Lab Evolution**: 8 what-if slider controls (Distance, Pullback, Reversal, Target%, Stop%, Trail%, Max Hold, Friction) with linked text inputs. Client-side simulation engine using 726-point 30d market data: SMA crossover + volume filter entry, target/stop/maxhold exit rules. Results: trade count, win rate, avg $ per trade. Trade overlay toggle shows both simulated (green/red dots on lab chart) and actual bot trades on 30d price chart. Built-in R11/R12 preset dropdown + save/load to localStorage. Full single-file deployment (43.5KB), 30/30 feature checks pass.

## Phase 3 User Report + Centralized Chart (V4) (2026-05-03 02:25-04:20 MT)
### Phase 3 Invisible
- User reports "not seeing them" — Phase 3 features (forecast, performance dashboard, strategy lab) not showing
- Server diagnosis confirmed ALL Phase 3 HTML elements present, JS functions defined, braces balanced
- Root cause was browser cache — user reluctant to hard refresh
- Eventually confirmed Phase 3 appeared in lab but: only red dots, no line chart, no bands, no live update, needed "show trade overlay" toggle

### V4 Centralized Single-Chart Architecture
- User requested: ALL info (forecast, performance, what-if overlays) rolled into ONE main chart as single operating surface
- Strategy lab must auto-run on slider change (debounced 300ms)
- One-shot deployment from /tmp/bloc_v4_central.html
- Architecture: removed signalChart canvas, consolidated all overlays into drawMainChart():
  - Legend (6 items) top-left
  - EMA Forecast annotation box top-right (delta, convergence tick/price projection)
  - Performance mini-card top-left below legend (win rate %, avg PnL, Sharpe)
  - WETH edge line on secondary Y-axis (purple, right side)
  - What-if trade overlay as transparent dots on main chart
  - Strategy lab chart (drawLab) kept as reference with price line
  - Auto-whatif on slider change: debounced 300ms
- Deployed to /var/www/bazaar/dashboard/public/bloc/index.html
- Restarted bazaar-dashboard service
- 45289 bytes, 244/244 brace balance

### V4 User Reports Blank Page
- User: "nope... instead everything is blanked out bro" — page completely blank/white
- Test page deployed at /bloc/test.html confirmed working ("I see it")
- Root cause: runtime JS error (not syntax) — proved by valid parse + blank render

## V4 Recovery & Phase 3 Restoration (2026-05-03 04:42-05:04 MDT)

### Bug 1: Signal Chart + Exec Chart Blank — `pts→p` Replace Overreach
- Reverted to Phase 3 working baseline from /tmp/bloc_phase3.html
- Edit 6's blind `pts→p` replace in `drawLines` function hit the parameter name `pts`, changing it to `p` which was out of scope, breaking ALL canvas drawing
- Fixed: reverted bad replace, restored `pts` as parameter name

### Bug 2: Only Main Price Line Visible — Missing `series` Return
- After Bug 1 fix: signal/exec charts still blank
- Root cause: `drawLines` return object lacks `series` key. `drawPriceChart` does `info.series.filter(...)` → TypeError → crashes load chain
- Fixed: added `series` to `drawLines` return object
- After fix: signal chart, exec chart, trade table, sparklines, all panels render fully
- Phases 1-3 all operational on Phase 3 foundation + centralized chart overlays
- Deployed as V5 to production

### Key Lesson
- Never blindly replace common JS vars (`pts`, `p`) across large files
- `drawLines` return must include `series` array — consuming code depends on it

## Afternoon Diagnostics & Fixes (2026-05-03 ~16:00-16:45 MDT)

### bloc_trace Staleness Discovered
- bloc_trace.jsonl last modified: May 2 21:54 UTC (16h+ stale at diagnosis time)
- Bot (PID 712226) was restarted May 3 05:14 UTC with R11 config — bloc_trace writer never resumed
- Bot process still has bloc_trace path defined in main.py, writes trace on every tick
- Root cause: bloc_trace writes via BLOC_TRACE_PATH.open('a') — file exists, bot runs, but 0 new lines since restart. Possible silent write failure or bot's tick cycle never reaches the trace block
- tick_deque.json and scalp_state.json update fine (both show tick data seconds old)
- stop_loss.log: last write May 1 — also stale but separate concern

### bloc_history API Fix
- Patched `/var/www/bazaar/dashboard/scripts/serve_dashboard.py` `bloc_history()` to supplement stale bloc_trace.jsonl with live data from tick_deque.json + scalp_state.json
- Merged live ticks: every tick in deque gets enriched with ema12, ema50, trigger, prime_buy, prime_sell, preferred_reentry, weth_edge_pct from scalp_state
- Timestamps synthesized: base=now−(deque_len×60s), each tick stepping forward 60s
- API now returns 720 points maximum — old bloc_trace data + latest 30 deque ticks on top
- Points include: ts, price, entry, ema_12, ema_50, trigger, prime_sell, prime_buy, preferred_reentry, weth_edge_pct, hold_state
- Verified: curl returns live data with last timestamp seconds old

### Frontend Timezone + Stale Data Fixes (deployed to /var/www/bazaar/dashboard/public/bloc/index.html)
- **X-axis timestamp labels**: drawLines() now draws 5 evenly-spaced HH:MM labels at bottom of price chart. Converted to America/Denver via toLocaleString(timeZone:'America/Denver')
- **Trade table timestamp column**: Added "Time" column header and timestamp cell in each row. Uses mt() helper = America/Denver
- **Stale data banner**: Red banner (#staleBanner) auto-shows when last tick >5min old, with "Data stale by X min" message. Hidden when fresh. CSS: #ff5d73 background, white text, 11px, positioned above chart row
- **CSS**: Added .trade-table td.ts formatting (9px, rgba(255,255,255,.4))
- Service restarted, verified live API response with 720 points, latest timestamp seconds old

### Bot State (verified 22:50 UTC)
- Side: USDC
- Entry: $2,300.605
- Current price: ~$2,341
- PnL: +$2.77 (53 trades)
- WETH: 0.0, USDC: ~$247
- Bot process running since 05:14 UTC (17h+ uptime)
- Journal entries streaming live (last entry seconds old via systemd journal)

## Bloc Dashboard Touch Handler Corruption & Fix (2026-05-03 17:00-17:30 MDT)

### Root Cause
- Bash `$()` command substitution in `scp` heredoc ate `$('marketChart')` from touch handlers, leaving bare `.addEventListener`
- This corrupted the touch handler IIFE, leaving a JS syntax error that blocked ALL rendering (blank chart)
- Multiple layers of patch accumulation (chart_overhaul.py → touch fix → recoloring → verification → more touch) compounded the corruption

### Progress Made
- **Chart overhaul applied**: Y-axis price labels, prime band fill (gold shading between buy/sell levels), thicker price lines, current price reference dashed line, crosshair with tooltip
- **Touch handlers deployed**: touchstart/touchmove/touchend handlers using `document.getElementById` (avoiding `$()` bash issue) with crosshair/tooltip
- **JS brace imbalance of +1 identified**: node --check on extracted JS confirmed "Unexpected end of input"
- **clean_rebuild.py written but not yet deployed**: removes all corrupted touch sections, adds clean handlers via Python string operations (no bash)

### Key Technical Insights
- Cannot use `$()` format in bash heredoc context on this deployment — bash interprets `$('...')` as command substitution
- Always write JS snippets as Python strings or escape `$` when deploying via heredoc
- chart_overhaul.py (50422 bytes working) is the last known good state — touch handlers need to be deployed on top of this cleanly
- Second Y-axis (WETH edge % on right side) is the remaining chart feature gap

### Remaining Tasks
- [ ] Deploy clean_rebuild.py fix to resolve the JS syntax error
- [ ] Verify chart renders with data, crosshair, and all panels
- [ ] Systemd service restart verification after deploy
- [ ] Add second Y-axis for WETH edge %

### Bot State
- Bot PID 712226, running 17h+ since last restart
- WETH side, 0.0 WETH held, ~$247 USDC
- PnL +$2.77 (53 trades)
- Dashboard API at /api/hq/status returns live data from tick_deque.json + scalp_state.json

## Bloc Dashboard JS Brace Imbalance Deep-Dive (2026-05-03 17:35-17:50 MDT)

### Progression of Analysis
1. **Brace/paren count**: Final balance was braces=+2, parens=+2 after `clean_rebuild.py`
2. **Key finding**: The `$('marketChart').addEventListener('mouseleave',function(){$('chartTip').style.display='none';const ctx=this.getContext('2d');` — 125 chars — was an **orphaned function** with NO closing `});`  
   - The closing braces/parens were eaten by the original bash `$()` corruption
   - Fix: append `drawPriceChart(history)});` to properly close the mouseleave handler
3. **Duplicate touch section**: Two identical `// ── Touch crosshair ──` IIFEs existed in the JS (one from clean_rebuild.py, the other a pre-existing corrupted copy)
4. **Nuclear fix applied**: 
   - `str.replace()` method closed mouseleave handler (but also matched a SECOND occurrence inside the corrupted touch section, causing duplicate `drawPriceChart(history)});`)
   - Regex `// ── Touch crosshair ──.*?(?=// ── Events ──|// Init)` correctly found and removed 1 touch section (4531 bytes)
   - Clean touch handlers re-inserted before Events section
5. **Post-nuclear state**: braces=+1, parens=+1, node check still fails at L347 (duplicate mouseleave close)

### Durable Lessons
- **`str.replace(old, new)` is dangerous** — it replaces ALL occurrences of the match string, not just the first. Use `str.replace(old, new, 1)` for single-replacement
- **Python regex `.*?` non-greedy match** correctly captures from one marker to the next, useful for removing corrupted sections
- **File size after interventions**: 53263 → 50971 → 51189 bytes (post nuclear fix)
- **Downloading file for local analysis**: `curl -s http://137.184.144.196/bloc/index.html > /tmp/bloc_current.html` avoids SSH round-trips for diagnostic work
- **LL rotation needed**: The Core and LL models are cycling through different reasoning approaches on the same problem — every turn shows fresh angle but no resolution. If blocked for multiple turns, need to build the file from scratch locally (download current → fix in Python → test locally → scp back)

### Blockers
- The +1 brace/+1 paren imbalance remains — suspected in Events section handling (unclosed arrow function from button handler) or in the duplication from str.replace() fixing both matches of the mouseleave pattern
- Dashboard remains blank/empty as shown in user's screenshot
- Need to download current file to local, inspect the exact L347 location, and fix precisely rather than applying another patch blind

### Full Timeline
- Started ~17:00 MDT with dashboard blank (no data rendered)
- Identified touch handler corruption (bash `$()` issue) at 17:05
- Fixed handlers but then discovered brace imbalance at 17:15
- Spent 17:15-17:35 doing incremental brace analysis (verify_js.py, find_js_imbalance.py, check_braces.py, etc.)
- clean_rebuild.py at 17:20 seemed promising but introduced second touch IIFE
- Nuclear fix at ~17:35 removed one touch section but mouseleave fix doubled
- Session compacted at 17:46 — still unresolved with braces=+1, parens=+1

### Open Questions for Next Session
1. What exactly is at JS L347 after the nuclear fix (the duplicated `drawPriceChart(history)});drawPriceChart(history)});`)?
2. Why does the touch section regex only find 1 section instead of 2?
3. Is the remaining +1 brace/paren from Events section buttons or from the IIFE duplicates?

## Dashboard Rescue Final Push (2026-05-03 17:57-18:07 MDT)

### Dashboard Rescued — Fixed and Deployed
- Two concrete bugs identified in post-nuclear-fix file:
  1. **L347 duplicate mouseleave close**: `drawPriceChart(history)});drawPriceChart(history)});` — `str.replace()` matched two mouseleave patterns (one in handler, one in corrupted touch section)
  2. **L377 truncated button handler**: Events section `querySelectorAll('.btn[data-range]')` arrow function was missing `b.classList.add('active');range=Number(b.dataset.range);load()}` — the closing statements were eaten by bash `$()` corruption
- Strategy: **Download current broken file → fix locally in Python → verify with `node --check` → scp back to VPS**. Avoids all bash/heredoc quoting issues.
- Local fix applied: removed duplicate mouseleave close, completed button handler, confirmed `node --check` PASS (b=0, p=0, s=0)
- Deployed 51064-byte fixed file to `/tmp/bloc_fixed.html` on VPS
- User ran `sudo cp && systemctl restart` on VPS

### Result
- **Chart now renders**: User confirmed they can see the graph with data
- **Invisible dotted lines**: Dash pattern alpha is too low (rgba(255,255,255,.06-.12)) — grids and crosshair barely visible
- **Performance & config panels empty**: `/api/hq/config?target=scalp` returns 404 (endpoint doesn't exist). `loadParams()` try/catch catches silently, leaving param controls unpopulated.
- **Threshold disagreement**: Bot has `REENTRY_SCORE_THRESHOLD = 0.45`, user says it should be 0.35 (agreed value)
- **Bot in USDC side**: Entry at $2,300.605, current $2,322, reentry score 0.3891 (below 0.45 threshold)

### Files Deployed
- `/tmp/bloc_brighter.html` (51064 bytes) — brighter dash pattern alpha values
- `/tmp/apply_all_fixes.py` — changes threshold to 0.35, deploys brighter dashboard, restarts services
- Awaiting user's `sudo python3 /tmp/apply_all_fixes.py`

### Key Lesson (again)
- For deployed JS fixes: download → fix locally in Python → verify with `node --check` → scp back. Never patch in place via shell heredocs or sed on the VPS — bash quoting and the `$()` issue will always cause corruption with jQuery-style selectors.
- When doing `str.replace()`, always use `str.replace(old, new, 1)` for single-replacement to avoid duplicate-matching bugs.
- The `$` alias (`const $=id=>document.getElementById(id)`) is properly defined and works — bash corruption was the root cause.
## Phase 4: Desert Theme + Contrast Overhaul (2026-05-03 18:10-18:55 MDT)

### REENTRY_SCORE_THRESHOLD Changed (0.45→0.35)
- Bot's main.py:73 had `REENTRY_SCORE_THRESHOLD = 0.45` but user confirmed agreed value was 0.35
- Fix bundled into `/tmp/apply_all_fixes.py` — deploys via sed on VPS
- Reentry score was 0.3891 — was below 0.45 but now above 0.35, qualifying for reentry

### Dashboard Desert Theme Deployed
- User ran `sudo python3 /tmp/apply_all_fixes.py` which deployed desert-themed bloc_desert.html
- CSS variables changed from dark theme to desert/cream palette:
  - `--bg: #FFFDD0` (cream page bg)
  - `--panel: #F5EED6` (cream sand panels)
  - `--panel-border: #D6C4A9` (sand border)
  - `--text: #2A1B1F` (dark chocolate text)
  - `--good: #1B7A51` (forest green), `--warn:#C8A35A` (gold), `--bad:#B11C1F` (carnelian)
  - `--blue:#5B7B8A` (slate), `--gold:#C8A35A`, `--orange:#B87C3A`, `--purple:#7A5C6B`
- User confirmed "Nice bro, that is better" for desert direction

### Contrast Iterations (3 rounds)
1. **fix_contrast.py** — target: every rgba(255,...) → chocolate. Changed grid alpha, labels, forecast/perf boxes. Canvas bg #080f1c → #E8DFC0. 9 replacements (7 missed — patterns outdated)
2. **fix_actual_colors.py** — target: the REAL legend color patterns in deployed file: `{get:p=>p.price,color:'#7ec8ff',...}` etc. Changed price (blue→chocolate), PrBuy (gold→forest), PrSell (green→carnelian), Rentry (orange→amber), Entry (white→teal). 5/6 matched (EMA12 pattern missed)
3. **fix_comp.py** — COMBINED: canvas bg→sand, grid→chocolate .12, Y/X labels→chocolate, legend text→chocolate, prime band→chocolate .04, current price ref (thicker), forecast/perf boxes→camel fill+gold border, delta text→chocolate bold 14px, crosshair→chocolate, WETH edge→thicker, legend colors fixed (5/6). 18 changes total, node PASS.

### Temperature Zone Shading + Gauge (add_temperature2.py)
- **Zone shading**: Chart bg gets green tint below prime_buy (accumulation zone) and red tint above prime_sell (distribution zone)
- **Temperature gauge**: Vertical gradient bar on right edge (red→gold→green) with current price dot marker
- **TEMP stat pill**: Shows ACCUMULATE (green pill), HOLDING (gold), or DISTRIBUTE (red) based on price vs prime band
- **Status**: Temperature version NOT yet deployed — user has not run the deploy command

### Deployment Pattern
- SCP-then-run: Write Python fix scripts locally on WSL → scp to VPS → user runs `sudo python3 /tmp/script.py` or `sudo cp /tmp/bloc_desert.html /var/www/bazaar/dashboard/public/bloc/index.html && sudo systemctl restart bazaar-dashboard`
- NEVER use bash heredocs or sed for JS deployment — `$()` bash corruption kills jQuery `$('#id')` selectors
- `apply_all_fixes.py` (on VPS at /tmp/) bundles: threshold change + dashboard deploy + service restart

### Key Technical
- `drawLines()` in JS renders: canvas bg, grid, axes, plot area, series, and returns `{px, py, mn, mx, series}`
- Series colors are defined in `drawPriceChart()` as array of `{get, color, dash?, w, l}` objects
- SVG-like canvas painting: fillStyle/strokeStyle set per draw call
- CSS custom properties control page-level theme; chart canvas has INDEPENDENT hardcoded colors

### Bot State (verified ~18:11 MDT)
- Side: USDC, entry: $2,300.605, current: $2,322.395
- Reentry score: 0.3891 (now qualifies after threshold 0.45→0.35)
- WETH edge: -0.05%, two-cycle edge: +0.09%
- Trades: 53, PnL: $2.77

### Pending
- Temperature zone shading + gauge + TEMP pill file at `/tmp/bloc_desert.html` — needs SCP and deploy
- User saw final contrast fix description (18 changes) but hasn't deployed yet
- `/api/hq/config?target=scalp` still returns 404 — config panel empty
- Image analysis API (moonshot/kimi-k2.5) suspended — 429 insufficient balance — can't analyze screenshots

### Cleanup
- `apply_all_fixes.py` at /tmp/ on VPS was left with old reference to bloc_brighter.html — updated to reference bloc_desert.html during fix_comp.py session
- Multiple stale versions in /tmp/ locally: fix_chart_desert.py, recolor_dashboard.py, recolor_desert.py, fix_contrast.py, fix_actual_colors.py, fix_comp.py, add_temperature.py, add_temperature2.py, extract_chart_colors.py, extract_colors.py, find_dollar.py — all disposable


## Temperature Gauge Repositioned (2026-05-03 ~19:05 MDT)
- **Problem**: Temperature gauge was a tiny 10px-wide vertical bar crammed at the right edge of the price chart (user: "jammed at the tail/end of the graph")
- **Fix**: Removed right-edge gauge entirely. Replaced with full-width horizontal temperature bar between stat pills row and the chart
- **CSS gradient track**: red (left) → gold (center) → green (right), labeled "HOT ← → COLD"
- **Diamond marker**: Shows current price position on the gradient, with smooth 0.5s CSS transition on position changes
- **Green mini-bar**: Marks prime buy level on the track
- **Label**: Shows current price and temperature zone (ACCUMULATE / HOLDING / DISTRIBUTE)
- **Deployment**: `/tmp/thermometer_fix.py` → SCP'd to VPS → needs `sudo cp && systemctl restart`
- **Scope**: 4 injections (CSS removal of old gauge + new CSS, HTML element, JS position updates), node check PASS, 54587 bytes

## Bot Idle Analysis (2026-05-03 ~19:14-19:21 MDT)
- **Bot alive**: PID 763846, running since ~05:14 UTC, journal streaming live, state file seconds fresh. Ticking every ~62s.
- **5 reasons bot sits idle (all valid)**:
  1. **Price at top of prime band**: current $2,316, prime sell $2,318 (only $2 above). Buying at resistance — correct to sit out.
  2. **EMAs converged**: EMA12 $2,315.92, EMA50 $2,315.36, delta +$0.44. No directional signal.
  3. **Declining volatility**: Volume 4.56% → 2.18% over 30min. Compression regime, low edge opportunity.
  4. **Marginal edge**: Edge est. $0.31 on $248 = 0.12%. WETH edge -0.05%, two-cycle +0.09%. Negligible after fees.
  5. **Wave quality below threshold**: 0.3378 (below 0.5) — choppy/noisy conditions, no clean setup.
- **No blocking flags**: recovery_mode=false, reentry_signal=false (correct — price above reentry zone $2,301), force_reentry=false, rollover_ready=false, continuation_hold=false.
- **Verdict**: Bot executing disciplined sit-out behavior. Entry requires dip toward prime buy ($2,290s), EMA separation, or volatility pickup. R11 tuning working as intended.

## Vigil + Athena Spec (2026-05-03 ~19:21 MDT)
### Vigil — Auto Fault Detector
- Lightweight companion process checking every 60s:
  - Bot process alive, state file <5min, internal ts <10min
  - Recovery mode not stuck >30min, tick deque fresh <5min
  - Disk space >10%, side/balance sanity check
- Aggregates to green/amber/red health status
- Writes `vigil_status.json` → read by dashboard API → shown as VIGIL pill on frontend

### Athena — Recursive Tuner
- Epoch-based optimization (default every 6h, cool-off prevents overfitting)
- Gather last 20 cycles → score current params → sweep ±1σ on each of 8 tuneable params → apply best dampened change
- Tuneable: STOP_LOSS, VOL_FLOOR, VOL_CAP, REENTRY_SCORE_THRESHOLD, TRIGGER_DELTA, PREMIUM_PCT, WAVE_THRESHOLD, ARM_WAIT_ALLOWED
- Score = 0.30×WinRate + 0.25×Sharpe + 0.20×Return + 0.15×MaxDrawdown(inv) + 0.10×TradeCount
- Safety rails: max 2 params/epoch, 15% range limit per change, auto-rollback on 10% score drop, manual pause flag
- Can run in simulate mode (dry-run recommendations only)

## Vigil Built & Uploaded (2026-05-03 ~19:28-19:33 MDT)
### Files Created (all on local WSL /tmp/):
- **vigil.py** (8739 bytes) — standalone monitor script. Checks bot_alive, state_fresh, recovery_stuck, tick_fresh, disk_ok, side_sanity. Runs as long-lived loop with 60s interval, writes vigil_status.json. Supports --once mode.
- **vigil.service** (459 bytes) — systemd unit. BindsTo=eth-scalper.service, auto-restart on failure, logs to /var/log/vigil.log.
- **deploy_vigil.py** (9948 bytes) — one-shot deploy script. Installs vigil.py, systemd service, patches serve_dashboard.py with /api/hq/vigil endpoint, adds vigil CSS/HTML/JS to frontend, starts services, verifies endpoint returns data.

### Deploy Pattern
- All 3 files SCP'd to VPS /tmp/ via SCP
- User runs: `sudo python3 /tmp/deploy_vigil.py`
- Script handles: file install, systemd setup, backend patch, frontend patch (CSS + HTML element + JS fetch loop), service start, verification

### Frontend Integration
- VIGIL pill added near TEMP pill in stat pills row
- Color-coded: green/amber/red/unknown with dot indicator
- Shows status + first failure/warning message (40 char truncated)
- Fetches /api/hq/vigil every render cycle
- Hovering shows full failure details in title attribute

### Pending
- User needs to run deploy command on VPS
- After Vigil is stood up, proceed to build Athena (recursive tuner)
### Depoly Vigil — Bug Fix Iterations (2026-05-03 19:33-19:55 MDT)
- **Bug 1: Route search string mismatch** — deploy script searched for double-quote `"/api/hq/status"` but serve_dashboard.py uses single quotes `'/api/hq/status'`. Fixed anchor string.
- **Bug 2: `\u` unicode escape in Python source** — JS regex `[\u2705\u{1F534}\u26A0]` embedded in Python string caused `unicodeescape` decode error when Python parsed the file. Fixed cycles:
  - Python's `\u` + `\u{1F534}` ES6 bracket unicode both crash Python `ast.parse()`
  - Final fix: replaced ALL unicode escapes with actual emoji characters ✅🔴⚠ in JS regex
- **Bug 3: Replaced `re.sub()` with `str.replace()`** to avoid `\u` interpretation issues
- **Bug 4: `\u{1F534}` (ES6 bracket unicode) not valid Python unicode** — replaced with literal 🔴 char
- **Deploy fix pattern**: Write fix locally, scp to VPS, verify `python3 -c "compile(open('/tmp/deploy_vigil.py').read(),'','exec')"` passes
- **Final deploy result**: 7 OK, 0 FAIL, 2 SKIP (vigilPanel already in HTML, no catch block in render)

### Vigil API Working + Schema Mismatch Fix (2026-05-03 19:45-19:55 MDT)
- Initial API at `/api/hq/vigil` returned 502 — indentation error in serve_dashboard.py route handler (16→8 spaces). Fixed by directly replacing malformed line.
- After fix: API returns response with `"status":"red"` — 3 false failures:
  - `tick_fresh`: "last tick 1777859147s ago" — tick_deque.json has no `ts` field per entry (plain array of prices), vigil was checking entry.ts
  - `state_fresh`: "unknown: no ts field" — scalp_state.json has no `ts` field, vigil expected one
  - `side_sanity`: "side=, weth=0.000000, usdc=$0.00" — schema has `side` but as empty string, balances under different keys
- **Fixed vigil.py** to match actual file schemas:
  - tick_fresh: use file mtime instead of entry.ts (entries are plain arrays)
  - state_fresh: accept missing `ts` field, check file age separately
  - recovery: use `last_flip` timestamp for idle detection
  - side_sanity: use actual keys (`last_weth_amount`, `last_usdc_amount`)
- **Deploy**: `sudo cp /tmp/vigil_fixed.py && systemctl restart vigil bazaar-dashboard`

### Vigil Green — All 6 Checks Passing (2026-05-03 ~19:55 MDT)
- `bot_alive`: ✅ service active
- `state_fresh`: ✅ 267s old (fresh)
- `tick_fresh`: ✅ deque 30 entries, 8s old
- `recovery`: ✅ normal
- `disk_ok`: ✅ 63% free
- `side_sanity`: ✅ side=USDC, 0 weth, $248

### Vigil Frontend JS Missing (2026-05-03 ~20:00-20:25 MDT)
- User: "don't see vigil anywhere" on dashboard
- Diagnosis: HTML element and CSS injected, but JS fetch/update logic NOT injected
- Deploy script hit "SKIP: no catch block in render" and fallback path didn't execute
- **Fix**: Wrote `/tmp/add_vigil_js.py` — patches the frontend HTML directly with a `setInterval()` loop that fetches `/api/hq/vigil` every 10s and updates the vigil pill
- User ran `sudo python3 /tmp/add_vigil_js.py && systemctl restart bazaar-dashboard`
- 5 instances of "vigil" in HTML (HTML+CSS only), but no JS fetch — at diagnosis time, no post-fix confirmation

### ETH Breakout Missed — Strategy Gap Identified (2026-05-03 20:25-20:55 MDT)
- ETH pumped ~$68 ($2,316→$2,384) while bot sat idle in USDC
- User frustrated: "look at price, it went up some $80 from when I last asked why we were holding USDC"
- Analysis confirmed bot was executing correctly for mean-reversion:
  - Price at top of prime band ($2,316 vs $2,318 prime sell)
  - EMAs converged (delta +$0.44, no directional signal)
  - Vol declining (4.56%→2.18%), wave quality 0.34 (below 0.5 threshold)
  - Entry required dip to $2,290s — that dip never came
- **Root cause**: Bot has only one mode (mean-reversion). When market breaks out of range, mean-reversion rejects the setup and sits out the entire move.
- **Proposed fix**: Trend-mode gate — second entry path:
  ```
  IF price > prime_sell by >5 points
     AND EMA12 > EMA50
     AND vol expanding:
    → BUY on pullback to EMA12 (not prime_buy)
  ```
- Bot side: USDC, entry $2,300.605, PnL +$2.77 (53 trades), $248 capital preserved (no loss)
- Missed opportunity: ~$7.20 on $248 with 3x (0.106 × $68)

### Key Technical Lessons (2026-05-03)
- **Python unicode in strings containing JS**: `\u` in Python source strings triggers `unicodeescape` decode at parse time. Use literal emoji or raw strings (`r"..."`) for JS snippets containing unicode regex.
- **SCP-then-run is the only safe deploy pattern** for JS files — bash heredocs with `$('id')` are eaten by command substitution
- **`str.replace(old, new)` replaces ALL occurrences** — use `str.replace(old, new, 1)` for single-target fixes
- **Bot mean-reversion logic is correct for range markets but blind to breakouts** — needs a second trend-following entry mode
- **Vigil state schema mismatches**: deploy_vigil.py assumed ts fields that don't exist in actual state JSONs. Always validate against live state file structure.
- **Frontend JS injection via Python deploy scripts**: Python strings embedding `<script>` with `\u` unicode escapes will crash. Use `\\u` (double backslash) or literal emoji.
- **ETH $2,300 level acts as support**: multiple attempts to dip there rejected. Prime buy set at $2,290, prime sell $2,318.

### Core Constants (verified)
- Bot PID was 712226 (restarted ~05:14 UTC), later 763846
- R11: STOP_LOSS 0.25, VOL_FLOOR 0.15, VOL_CAP 0.25, REENTRY_SCORE_THRESHOLD 0.35
- Dashboard: `/var/www/bazaar/dashboard/public/bloc/index.html`
- Serve script: `/var/www/bazaar/dashboard/scripts/serve_dashboard.py`
- State: `/var/www/bazaar/eth_scalper/state/scalp_state.json`
- Ticks: `/var/www/bazaar/eth_scalper/state/tick_deque.json`
- VPS IP: 137.184.144.196, user: alfred-deploy
- Wallet: 0xff9cc57be71a7b851a422a851600ac23b3843e27 (Base chain 8453)
- Bot tick interval: ~62s

## Trend-Mode Gate Implemented & Deployed (2026-05-03 20:55-21:04 MDT)
- **Issue**: ETH pumped ~$68 ($2,316→$2,384) while bot sat in USDC — mean-reversion strategy has no trend-following mode for breakouts
- **Fix**: Added `trend_entry_signal` as the first match in `buy_signal` OR gate:
  - Trend mode active when: EMA12 > EMA50 AND price > EMA12 AND vol ≥ 0.15%
  - Trend entry: price within 0.15% of EMA12 (a ~$3.6 pullback at $2,384)
  - Price must be below exit target (not buying at resistance)
  - Fee/weth gates must still pass
  - Logged as `BUY-TREND (EMA <value>)` to identify trend-mode entries
- **Deployment**: Created `/tmp/eth_scalper_trend.py` (modified local copy) → SCP'd to VPS → user ran `sudo cp` to overwrite main.py + `sudo systemctl restart eth-scalper.service`
- **Verification**: Bot restarted at 03:04 UTC, PID 773809, journal shows live startup with EMA12=2374.37, EMA50=2378.04
- **Current state**: Trend mode inactive (EMA12 < EMA50 in consolidation) — correct behavior
- Backed up original main.py to main.py.bak

## Vigil Frontend JS Fixes (2026-05-03 20:48-21:44 MDT)
- **Bug**: User reports vigil not visible on dashboard after deploy script ran
- **Diagnosis round 1 (20:48)**: grep found `vigilPanel` 5 times in HTML, but JS fetch/update logic missing. Deploy script hit SKIP ("no catch block in render found"), fallback path empty.
- **Fix 1**: Wrote `/tmp/add_vigil_js.py` (1584 bytes) — patches frontend HTML with `setInterval()` fetch loop targeting `/api/hq/vigil` every 10s. Syntax OK, uploaded. User needs to run `sudo python3 /tmp/add_vigil_js.py`.
- **Diagnosis round 2 (21:08-21:42)**: After user ran fix, vigil still not visible (hard refresh + incognito confirmed). Screenshot shows no vigil pill in stat pills row.
  - Root cause: deploy_vigil.py SKIP'd on `if 'vigilPanel' not in html` — the CSS string `vigilPanel { ... computed-style-name ... }` matched, so the inject was skipped. The HTML element was NEVER in the body.
  - grep confirmed 0 `id="vigilPanel"` elements in body HTML (only CSS rule text)
- **Fix 2**: Created `/tmp/vigil_fixed.html` — injects `<span id="vigilPanel" class="pill unknown" title="Vigil: waiting...">⏳ Vigil</span>` after FRESH chip's closing `</span>` tag
  - User ran: `sudo cp /tmp/vigil_fixed.html /var/www/bazaar/dashboard/public/bloc/index.html && sudo systemctl restart bazaar-dashboard`
- **Post-fix verification**: grep confirmed vigilPanel element now in body HTML. But user still reports at 21:44 MDT "not there" + "price below both EMAs, still holding USDC"

## Bot State at End of Session (2026-05-03 21:44 MDT)
- **Side**: USDC (correct to be out — EMAs bearish cross)
- **Entry**: $2,300.605
- **Price**: $2,380 | EMA12: $2,378 | EMA50: $2,380
- **EMAs**: Just crossed bearish (12 < 50)
- **PnL**: +$2.77 (53 trades)
- **Wallet**: ~$248 USDC
- **Trend mode**: Inactive (EMA12 < EMA50 — consolidation regime)
- **Bot PID**: 773809, running since 03:04 UTC
- **Entry target for mean-reversion**: $2,375 (dip needed)
- **Trend entry target**: Not applicable (EMAs bearish)
- **Vigil API**: Returns green — all 6 checks pass
- **Vigil frontend**: HTML element injected, user still reports not visible (possible browser cache or runtime JS error)

## Key Technical Lessons (Session 2)
- **Deploy script SKIP logic bug**: `if 'vigilPanel' not in html` matched CSS selector text `#vigilPanel` inside a `<style>` block, causing the actual HTML element injection to be skipped. Always scope element-existence checks to body content only, or check `id="vigilPanel"` specifically.
- **Trend-mode gate deploy went smoothly** — Python-to-Python copy on VPS (no bash heredoc issues) was clean. The code is correct; it just needs the right market conditions to activate.
- **Bot behavior is correct for current regime** — The missed breakout was painful but the bot is now correctly sitting out a bearish EMA cross. Chasing late would have been worse.

## Late-Night Session: Bot Enters WETH + Athena Build (21:30-22:10 MDT)

### Entry Fixes Applied (v3)
- **Root cause bot stuck in USDC**: `MIN_WETH_ACCUMULATION_PCT = 0.05` threshold too high for tight consolidation (actual edge ~0.038%)
- **Bug**: `expected_weth_after_roundtrip()` for USDC side buys at current `price` instead of `entry_target`, always showing negative edge (~−0.05%)
- **Fix 1**: `MIN_WETH_ACCUMULATION_PCT` lowered from 0.05 → 0.01
- **Fix 2**: USDC roundtrip now buys at `entry_target`, sells at `exit_price`
- **Fix 3**: `weth_ok` removed from `convergence_entry_signal` — `fee_ok` alone gates entry in consolidation
- **Convergence entry signal**: When EMAs within 0.15% spread, buy on EMA12 touch (within 0.08%). Catches small oscillations mean-reversion misses.
- **Trend-mode entry**: Buy on pullback to EMA12 during EMA12>EMA50 uptrends. Pullback tolerance = 0.15%. Trend mode gates: EMA12 > EMA50 AND price above EMA12 AND vol ≥ VOL_FLOOR AND recovery_mode == False. Rejects buys at sell target (`p < exit_price` check).
- **Sell/guardrail code untouched**: Convergence and trend-mode additions were entry-only. Continuation, rollover, extension, momentum, stop, and all sell logic unchanged.

### Bot Enters WETH (~21:48 MDT)
- State shifted from USDC to WETH: trade count 53→54
- Entry: ~$2,380 area (convergence buy at EMA12 touch)
- Current: ~$2,386.91 (+~$0.30 unrealized)
- EMA12: $2,387 | EMA50: $2,386 — bullish cross intact
- Exit target: $2,390.56 (0.15% trigger)
- Stop: ~$2,380 (0.25% below EMA50)

### Athena Tuner Built
- `athena.py` (12KB) created and uploaded to `/tmp/`, copied to `/var/www/bazaar/eth_scalper/`
- First epoch in simulate mode: 53 trades read, score = 0.19, no candidate improves (dataset dominated by stale May-2 data)
- Athena reads from `logs/trades.jsonl`, writes state to `state/` and config to `config/athena_config.json`
- Backend tuner only — no frontend rendering yet
- Next: schedule 6h cron epoch cycle after more fresh trade data

### Dashboard Data Sources Fix
- Dashboard reads `cycle_ledger.jsonl` (41 entries, stale May 2) for trade history and execution feed
- Bot writes fresh trades to `trades.jsonl` (53 entries, live)
- Created patch at `/tmp/fix_dashboard_data_sources.py` to make:
  - `hq_execution_feed` read from `trades.jsonl` (fresh) as primary source
  - `hq_trade_history` include `fresh_trades` array from `trades.jsonl`
  - `trade_log_count` added to API response
- Patch uploaded to VPS at `/tmp/serve_dashboard_fixed.py`
- Blocked on user deploy: `sudo cp /tmp/serve_dashboard_fixed.py /var/www/bazaar/dashboard/scripts/serve_dashboard.py && sudo systemctl restart bazaar-dashboard`

### Persistent Issue: bloc_trace + status file not updating
- `bloc_trace.jsonl` and `eth_scalper_status.json` last modified May 2 21:54 UTC (before restart)
- Both are supposed to write every 6th tick in the main loop
- Bot IS cycling (trades.jsonl, scalp_state.json, tick_deque.json all fresh)
- Suspect: JSON serialization of `status_payload` in the `tick % 6 == 0` block silently fails before hitting the file write
- Workaround: dashboard reads from fresh sources instead (above fix)

### Bot State at Session End (22:10 MDT)
- Side: WETH | Streak: 0 | Trades: 54 | PnL: $2.77
- Entry: $2,385.70 | Price: $2,386.91
- EMA12: $2,386.97 | EMA50: $2,386.01
- Exit target: $2,390.56 | Stop: ~$2,380
- Athena epoch: score 0.19, 53 trades read

### User Commands Executed
- `sudo mkdir -p /var/www/bazaar/eth_scalper/state && sudo cp /tmp/athena.py /var/www/bazaar/eth_scalper/` (Athena deployed)
- `cd /var/www/bazaar/eth_scalper && sudo python3 athena.py --simulate` (first epoch run)
- `sudo python3 /tmp/check_state.py` (state check: WETH confirmed)

### File Manifest (Late-night session)
- `/tmp/eth_scalper_v3.py` — convergence + trend entry + fix pack
- `/tmp/athena.py` — tuner engine (12KB)
- `/tmp/fix_dashboard_data_sources.py` — patch for serve_dashboard.py
- `/tmp/serve_dashboard_fixed.py` — patched serve_dashboard.py (awaiting deploy)
- `/tmp/check_state2.py` — detailed state reader
